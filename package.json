{
  "name": "konva-site-2",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "docusaurus build && node scripts/generate-llms.js",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "typecheck": "tsc",
    "check:i18n-drift": "node scripts/check-i18n-drift.js",
    "check:code-drift": "node scripts/check-code-drift.js",
    "check:fences": "node scripts/check-fences.js",
    "panel:run": "node scripts/llm-panel/run.js",
    "panel:compare": "node scripts/llm-panel/compare.js",
    "update:i18n-sources": "node scripts/check-i18n-drift.js --write && node scripts/check-code-drift.js --write",
    "check": "npm run check:i18n-drift && npm run check:code-drift && npm run check:fences && npm run typecheck && npm run build && node checksitemap.js && node checksitemap.js build/zh-Hans/sitemap.xml && npm run check:llms",
    "check:llms": "node scripts/check-llms.js"
  },
  "dependencies": {
    "@codesandbox/sandpack-react": "^2.20.0",
    "@docusaurus/core": "3.9.1",
    "@docusaurus/preset-classic": "3.9.1",
    "@mdx-js/react": "^3.1.1",
    "clsx": "^2.1.1",
    "disqus-react": "^1.1.7",
    "jsdoc-api": "^9.3.4",
    "konva": "^10.3.1",
    "prism-react-renderer": "^2.4.1",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.9.1",
    "@docusaurus/tsconfig": "3.9.1",
    "@docusaurus/types": "3.9.1",
    "typescript": "~5.9.2"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=18.0"
  }
}
