---
title: HTML5 Canvas 矩形教程
sidebar_label: 矩形
hide_table_of_contents: true
slug: Rect.html
description: "学习如何使用 Konva.js 在 HTML5 Canvas 上绘制矩形。使用 Konva.Rect 图形设置填充、描边、尺寸、位置、圆角、阴影等属性。"
---

要使用 `Konva` 创建矩形，可以实例化一个 `Konva.Rect()` 对象。

有关完整的属性和方法列表，请参阅 [Konva.Rect 文档](/api/Konva.Rect.html)。

你可以为 `Konva.Rect` 定义圆角半径。该值可以是一个数字，也可以是数字数组 `[topLeft, topRight, bottomRight, bottomLeft]`。

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

<Tabs>
  <TabItem value="Vanilla" default>
```js live vanilla
import Konva from 'konva';

const stage = new Konva.Stage({
container: 'container', // id of container <div>
width: window.innerWidth,
height: window.innerHeight
});

const layer = new Konva.Layer();
stage.add(layer);

const rect1 = new Konva.Rect({
x: 20,
y: 20,
width: 100,
height: 50,
fill: 'green',
stroke: 'black',
strokeWidth: 4
});
layer.add(rect1);

const rect2 = new Konva.Rect({
x: 150,
y: 40,
width: 100,
height: 50,
fill: 'red',
shadowBlur: 10,
cornerRadius: 10
});
layer.add(rect2);

const rect3 = new Konva.Rect({
x: 50,
y: 120,
width: 100,
height: 100,
fill: 'blue',
cornerRadius: [0, 10, 20, 30]
});
layer.add(rect3);

stage.add(layer);

```

</TabItem>
<TabItem value="React">
```js live react
import { Stage, Layer, Rect } from 'react-konva';

const App = () => {
  return (
    <Stage width={window.innerWidth} height={window.innerHeight}>
      <Layer>
        <Rect
          x={20}
          y={20}
          width={100}
          height={50}
          fill="green"
          stroke="black"
          strokeWidth={4}
        />
        <Rect
          x={150}
          y={40}
          width={100}
          height={50}
          fill="red"
          shadowBlur={10}
          cornerRadius={10}
        />
        <Rect
          x={50}
          y={120}
          width={100}
          height={100}
          fill="blue"
          cornerRadius={[0, 10, 20, 30]}
        />
      </Layer>
    </Stage>
  );
};

export default App;

```

</TabItem>
<TabItem value="Vue">
```js live vue
<template>
  <v-stage :config="stageSize">
    <v-layer>
      <v-rect :config="rect1Config" />
      <v-rect :config="rect2Config" />
      <v-rect :config="rect3Config" />
    </v-layer>
  </v-stage>
</template>

<script setup>
const stageSize = {
  width: window.innerWidth,
  height: window.innerHeight
};

const rect1Config = {
  x: 20,
  y: 20,
  width: 100,
  height: 50,
  fill: 'green',
  stroke: 'black',
  strokeWidth: 4
};

const rect2Config = {
  x: 150,
  y: 40,
  width: 100,
  height: 50,
  fill: 'red',
  shadowBlur: 10,
  cornerRadius: 10
};

const rect3Config = {
  x: 50,
  y: 120,
  width: 100,
  height: 100,
  fill: 'blue',
  cornerRadius: [0, 10, 20, 30]
};
</script>

```
</TabItem>
</Tabs>

**后续步骤：**

- [为图形添加事件 →](/docs/events/Binding_Events.html)
- [使图形可拖动 →](/docs/drag_and_drop/Drag_and_Drop.html)
- [添加调整尺寸和旋转手柄 →](/docs/select_and_transform/Basic_demo.html)
- [为图形属性添加动画 →](/docs/animations/Create_an_Animation.html)
- [Konva.Rect API 参考 →](/api/Konva.Rect.html)
