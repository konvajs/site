---
title: HTML5 Canvas Blob 图形教程
sidebar_label: 线条 - Blob 图形
hide_table_of_contents: true
slug: /docs/shapes/Line_-_Blob.html
description: "学习如何使用 Konva 在 HTML5 Canvas 上绘制 Blob 图形，以及如何为 Konva.Line 使用 closed 和 tension 属性。"
---

要使用 `Konva` 创建 Blob 图形，可以实例化一个 `Konva.Line()` 对象，并同时设置 `closed` 和 `tension` 属性。

有关完整的属性和方法列表，请参阅 [Line API 参考](/api/Konva.Line.html)。

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

<Tabs>
  <TabItem value="Vanilla" default>
```js live vanilla
import Konva from 'konva';

const stage = new Konva.Stage({
  container: 'container',
  width: window.innerWidth,
  height: window.innerHeight
});

const layer = new Konva.Layer();
stage.add(layer);

const blob = new Konva.Line({
  points: [23, 20, 23, 160, 70, 93, 150, 109, 290, 139, 270, 93],
  fill: '#00D2FF',
  stroke: 'black',
  strokeWidth: 5,
  closed: true,
  tension: 0.3
});

layer.add(blob);
```
  </TabItem>
  <TabItem value="React">
```js live react
import { Stage, Layer, Line } from 'react-konva';

const App = () => {
  return (
    <Stage width={window.innerWidth} height={window.innerHeight}>
      <Layer>
        <Line
          points={[23, 20, 23, 160, 70, 93, 150, 109, 290, 139, 270, 93]}
          fill="#00D2FF"
          stroke="black"
          strokeWidth={5}
          closed
          tension={0.3}
        />
      </Layer>
    </Stage>
  );
};

export default App;
```
  </TabItem>
  <TabItem value="Vue">
```js live vue
<template>
  <v-stage :config="stageSize">
    <v-layer>
      <v-line :config="blobConfig" />
    </v-layer>
  </v-stage>
</template>

<script setup>
const stageSize = {
  width: window.innerWidth,
  height: window.innerHeight
};

const blobConfig = {
  points: [23, 20, 23, 160, 70, 93, 150, 109, 290, 139, 270, 93],
  fill: '#00D2FF',
  stroke: 'black',
  strokeWidth: 5,
  closed: true,
  tension: 0.3
};
</script>
```
  </TabItem>
</Tabs> 
