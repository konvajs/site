---
title: HTML5 Canvas 圆环教程
sidebar_label: 圆环
hide_table_of_contents: true
slug: /docs/shapes/Ring.html
description: "学习如何使用 Konva.js 在 HTML5 Canvas 上绘制圆环。使用 Konva.Ring 图形设置内半径和外半径。"
---

要使用 `Konva` 创建圆环，可以使用 `Konva.Ring()` 对象。

有关完整的属性和方法列表，请参阅 [Ring API 参考](/api/Konva.Ring.html)。

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

<Tabs>
  <TabItem value="Vanilla" default>
```js live vanilla
import Konva from 'konva';

const stage = new Konva.Stage({
  container: 'container',
  width: window.innerWidth,
  height: window.innerHeight
});

const layer = new Konva.Layer();
stage.add(layer);

const ring = new Konva.Ring({
  x: stage.width() / 2,
  y: stage.height() / 2,
  innerRadius: 40,
  outerRadius: 70,
  fill: 'yellow',
  stroke: 'black',
  strokeWidth: 4
});

layer.add(ring);
```
  </TabItem>
  <TabItem value="React">
```js live react
import { Stage, Layer, Ring } from 'react-konva';

const App = () => {
  return (
    <Stage width={window.innerWidth} height={window.innerHeight}>
      <Layer>
        <Ring
          x={window.innerWidth / 2}
          y={window.innerHeight / 2}
          innerRadius={40}
          outerRadius={70}
          fill="yellow"
          stroke="black"
          strokeWidth={4}
        />
      </Layer>
    </Stage>
  );
};

export default App;
```
  </TabItem>
  <TabItem value="Vue">
```js live vue
<template>
  <v-stage :config="stageSize">
    <v-layer>
      <v-ring :config="ringConfig" />
    </v-layer>
  </v-stage>
</template>

<script setup>
const stageSize = {
  width: window.innerWidth,
  height: window.innerHeight
};

const ringConfig = {
  x: window.innerWidth / 2,
  y: window.innerHeight / 2,
  innerRadius: 40,
  outerRadius: 70,
  fill: 'yellow',
  stroke: 'black',
  strokeWidth: 4
};
</script>
```
  </TabItem>
</Tabs> 
