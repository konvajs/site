<a name="Konva.Circle"></a>

## Konva.Circle ⇐ [<code>Shape</code>](#Konva.Shape)
Circle constructor

**Extends**: [<code>Shape</code>](#Konva.Shape)  

* [.Circle](#Konva.Circle) ⇐ [<code>Shape</code>](#Konva.Shape)
    * [new Circle(config)](#new_Konva.Circle_new)
    * [.radius(radius)](#Konva.Circle+radius) ⇒ <code>Number</code>
    * [.hasShadow()](#Konva.Shape+hasShadow) ⇒ <code>Boolean</code>
    * [.hasFill()](#Konva.Shape+hasFill) ⇒ <code>Boolean</code>
    * [.hasStroke()](#Konva.Shape+hasStroke) ⇒ <code>Boolean</code>
    * [.intersects(point)](#Konva.Shape+intersects) ⇒ <code>Boolean</code>
    * [.getSelfRect()](#Konva.Shape+getSelfRect) ⇒ <code>Object</code>
    * [.drawHitFromCache(alphaThreshold)](#Konva.Shape+drawHitFromCache) ⇒ [<code>Shape</code>](#Konva.Shape)
    * [.stroke(color)](#Konva.Shape+stroke) ⇒ <code>String</code>
    * [.strokeWidth(strokeWidth)](#Konva.Shape+strokeWidth) ⇒ <code>Number</code>
    * [.fillAfterStrokeEnabled(fillAfterStrokeEnabled)](#Konva.Shape+fillAfterStrokeEnabled) ⇒ <code>Boolean</code>
    * [.hitStrokeWidth(hitStrokeWidth)](#Konva.Shape+hitStrokeWidth) ⇒ <code>Number</code>
    * [.strokeHitEnabled(strokeHitEnabled)](#Konva.Shape+strokeHitEnabled) ⇒ <code>Boolean</code>
    * [.perfectDrawEnabled(perfectDrawEnabled)](#Konva.Shape+perfectDrawEnabled) ⇒ <code>Boolean</code>
    * [.shadowForStrokeEnabled(shadowForStrokeEnabled)](#Konva.Shape+shadowForStrokeEnabled) ⇒ <code>Boolean</code>
    * [.lineJoin(lineJoin)](#Konva.Shape+lineJoin) ⇒ <code>String</code>
    * [.lineCap(lineCap)](#Konva.Shape+lineCap) ⇒ <code>String</code>
    * [.sceneFunc(drawFunc)](#Konva.Shape+sceneFunc) ⇒ <code>function</code>
    * [.hitFunc(drawFunc)](#Konva.Shape+hitFunc) ⇒ <code>function</code>
    * [.dash(dash)](#Konva.Shape+dash) ⇒ <code>Array</code>
    * [.shadowColor(color)](#Konva.Shape+shadowColor) ⇒ <code>String</code>
    * [.shadowBlur(blur)](#Konva.Shape+shadowBlur) ⇒ <code>Number</code>
    * [.shadowOpacity(opacity)](#Konva.Shape+shadowOpacity) ⇒ <code>Number</code>
    * [.shadowOffset(offset)](#Konva.Shape+shadowOffset) ⇒ <code>Object</code>
    * [.shadowOffsetX(x)](#Konva.Shape+shadowOffsetX) ⇒ <code>Number</code>
    * [.shadowOffsetY(y)](#Konva.Shape+shadowOffsetY) ⇒ <code>Number</code>
    * [.fillPatternImage(image)](#Konva.Shape+fillPatternImage) ⇒ <code>Image</code>
    * [.fill(color)](#Konva.Shape+fill) ⇒ <code>String</code>
    * [.fillPatternX(x)](#Konva.Shape+fillPatternX) ⇒ <code>Number</code>
    * [.fillPatternY(y)](#Konva.Shape+fillPatternY) ⇒ <code>Number</code>
    * [.fillLinearGradientColorStops(colorStops)](#Konva.Shape+fillLinearGradientColorStops) ⇒ <code>Array</code>
    * [.strokeLinearGradientColorStops(colorStops)](#Konva.Shape+strokeLinearGradientColorStops) ⇒ <code>Array</code>
    * [.fillRadialGradientStartRadius(radius)](#Konva.Shape+fillRadialGradientStartRadius) ⇒ <code>Number</code>
    * [.fillRadialGradientEndRadius(radius)](#Konva.Shape+fillRadialGradientEndRadius) ⇒ <code>Number</code>
    * [.fillRadialGradientColorStops(colorStops)](#Konva.Shape+fillRadialGradientColorStops) ⇒ <code>Array</code>
    * [.fillPatternRepeat(repeat)](#Konva.Shape+fillPatternRepeat) ⇒ <code>String</code>
    * [.fillEnabled(enabled)](#Konva.Shape+fillEnabled) ⇒ <code>Boolean</code>
    * [.strokeEnabled(enabled)](#Konva.Shape+strokeEnabled) ⇒ <code>Boolean</code>
    * [.shadowEnabled(enabled)](#Konva.Shape+shadowEnabled) ⇒ <code>Boolean</code>
    * [.dashEnabled(enabled)](#Konva.Shape+dashEnabled) ⇒ <code>Boolean</code>
    * [.strokeScaleEnabled(enabled)](#Konva.Shape+strokeScaleEnabled) ⇒ <code>Boolean</code>
    * [.fillPriority(priority)](#Konva.Shape+fillPriority) ⇒ <code>String</code>
    * [.fillPatternOffset(offset)](#Konva.Shape+fillPatternOffset) ⇒ <code>Object</code>
    * [.fillPatternOffsetX(x)](#Konva.Shape+fillPatternOffsetX) ⇒ <code>Number</code>
    * [.fillPatternOffsetY(y)](#Konva.Shape+fillPatternOffsetY) ⇒ <code>Number</code>
    * [.fillPatternScale(scale)](#Konva.Shape+fillPatternScale) ⇒ <code>Object</code>
    * [.fillPatternScaleX(x)](#Konva.Shape+fillPatternScaleX) ⇒ <code>Number</code>
    * [.fillPatternScaleY(y)](#Konva.Shape+fillPatternScaleY) ⇒ <code>Number</code>
    * [.fillLinearGradientStartPoint(startPoint)](#Konva.Shape+fillLinearGradientStartPoint) ⇒ <code>Object</code>
    * [.strokeLinearGradientStartPoint(startPoint)](#Konva.Shape+strokeLinearGradientStartPoint) ⇒ <code>Object</code>
    * [.fillLinearGradientStartPointX(x)](#Konva.Shape+fillLinearGradientStartPointX) ⇒ <code>Number</code>
    * [.linearLinearGradientStartPointX(x)](#Konva.Shape+linearLinearGradientStartPointX) ⇒ <code>Number</code>
    * [.fillLinearGradientStartPointY(y)](#Konva.Shape+fillLinearGradientStartPointY) ⇒ <code>Number</code>
    * [.strokeLinearGradientStartPointY(y)](#Konva.Shape+strokeLinearGradientStartPointY) ⇒ <code>Number</code>
    * [.fillLinearGradientEndPoint(endPoint)](#Konva.Shape+fillLinearGradientEndPoint) ⇒ <code>Object</code>
    * [.strokeLinearGradientEndPoint(endPoint)](#Konva.Shape+strokeLinearGradientEndPoint) ⇒ <code>Object</code>
    * [.fillLinearGradientEndPointX(x)](#Konva.Shape+fillLinearGradientEndPointX) ⇒ <code>Number</code>
    * [.strokeLinearGradientEndPointX(x)](#Konva.Shape+strokeLinearGradientEndPointX) ⇒ <code>Number</code>
    * [.fillLinearGradientEndPointY(y)](#Konva.Shape+fillLinearGradientEndPointY) ⇒ <code>Number</code>
    * [.strokeLinearGradientEndPointY(y)](#Konva.Shape+strokeLinearGradientEndPointY) ⇒ <code>Number</code>
    * [.fillRadialGradientStartPoint(startPoint)](#Konva.Shape+fillRadialGradientStartPoint) ⇒ <code>Object</code>
    * [.fillRadialGradientStartPointX(x)](#Konva.Shape+fillRadialGradientStartPointX) ⇒ <code>Number</code>
    * [.fillRadialGradientStartPointY(y)](#Konva.Shape+fillRadialGradientStartPointY) ⇒ <code>Number</code>
    * [.fillRadialGradientEndPoint(endPoint)](#Konva.Shape+fillRadialGradientEndPoint) ⇒ <code>Object</code>
    * [.fillRadialGradientEndPointX(x)](#Konva.Shape+fillRadialGradientEndPointX) ⇒ <code>Number</code>
    * [.fillRadialGradientEndPointY(y)](#Konva.Shape+fillRadialGradientEndPointY) ⇒ <code>Number</code>
    * [.fillPatternRotation(rotation)](#Konva.Shape+fillPatternRotation) ⇒ [<code>Shape</code>](#Konva.Shape)
    * [.fillRule(rotation)](#Konva.Shape+fillRule) ⇒ [<code>Shape</code>](#Konva.Shape)
    * [.clearCache()](#Konva.Node+clearCache) ⇒ [<code>Node</code>](#Konva.Node)
    * [.cache([config])](#Konva.Node+cache) ⇒ [<code>Node</code>](#Konva.Node)
    * [.isCached()](#Konva.Node+isCached) ⇒ <code>Boolean</code>
    * [.getClientRect(config)](#Konva.Node+getClientRect) ⇒ <code>Object</code>
    * [.on(evtStr, handler)](#Konva.Node+on) ⇒ [<code>Node</code>](#Konva.Node)
    * [.off(evtStr)](#Konva.Node+off) ⇒ [<code>Node</code>](#Konva.Node)
    * [.remove()](#Konva.Node+remove) ⇒ [<code>Node</code>](#Konva.Node)
    * [.destroy()](#Konva.Node+destroy)
    * [.getAttr(attr)](#Konva.Node+getAttr) ⇒ <code>Integer</code> \| <code>String</code> \| <code>Object</code> \| <code>Array</code>
    * [.getAncestors()](#Konva.Node+getAncestors) ⇒ <code>Array</code>
    * [.getAttrs()](#Konva.Node+getAttrs) ⇒ <code>Object</code>
    * [.setAttrs(config)](#Konva.Node+setAttrs) ⇒ [<code>Node</code>](#Konva.Node)
    * [.isListening()](#Konva.Node+isListening) ⇒ <code>Boolean</code>
    * [.isVisible()](#Konva.Node+isVisible) ⇒ <code>Boolean</code>
    * [.show()](#Konva.Node+show) ⇒ [<code>Node</code>](#Konva.Node)
    * [.hide()](#Konva.Node+hide) ⇒ [<code>Node</code>](#Konva.Node)
    * [.getAbsoluteZIndex()](#Konva.Node+getAbsoluteZIndex) ⇒ <code>Integer</code>
    * [.getDepth()](#Konva.Node+getDepth) ⇒ <code>Integer</code>
    * [.getRelativePointerPosition()](#Konva.Node+getRelativePointerPosition) ⇒ [<code>Node</code>](#Konva.Node)
    * [.getAbsolutePosition(Ancestor)](#Konva.Node+getAbsolutePosition) ⇒ [<code>Node</code>](#Konva.Node)
    * [.move(change)](#Konva.Node+move) ⇒ [<code>Node</code>](#Konva.Node)
    * [.rotate(theta)](#Konva.Node+rotate) ⇒ [<code>Node</code>](#Konva.Node)
    * [.moveToTop()](#Konva.Node+moveToTop) ⇒ <code>Boolean</code>
    * [.moveUp()](#Konva.Node+moveUp) ⇒ <code>Boolean</code>
    * [.moveDown()](#Konva.Node+moveDown) ⇒ <code>Boolean</code>
    * [.moveToBottom()](#Konva.Node+moveToBottom) ⇒ <code>Boolean</code>
    * [.getAbsoluteOpacity()](#Konva.Node+getAbsoluteOpacity) ⇒ <code>Number</code>
    * [.moveTo(newContainer)](#Konva.Node+moveTo) ⇒ [<code>Node</code>](#Konva.Node)
    * [.toObject()](#Konva.Node+toObject) ⇒ <code>Object</code>
    * [.toJSON()](#Konva.Node+toJSON) ⇒ <code>String</code>
    * [.getParent()](#Konva.Node+getParent) ⇒ [<code>Node</code>](#Konva.Node)
    * [.findAncestors(selector, [includeSelf], [stopNode])](#Konva.Node+findAncestors) ⇒ <code>Array</code>
    * [.findAncestor(selector, [includeSelf], [stopNode])](#Konva.Node+findAncestor) ⇒ [<code>Node</code>](#Konva.Node)
    * [.getLayer()](#Konva.Node+getLayer) ⇒ [<code>Layer</code>](#Konva.Layer)
    * [.getStage()](#Konva.Node+getStage) ⇒ [<code>Stage</code>](#Konva.Stage)
    * [.fire(eventType, [evt], [bubble])](#Konva.Node+fire) ⇒ [<code>Node</code>](#Konva.Node)
    * [.getAbsoluteTransform()](#Konva.Node+getAbsoluteTransform) ⇒ [<code>Transform</code>](#Konva.Transform)
    * [.getAbsoluteScale()](#Konva.Node+getAbsoluteScale) ⇒ <code>Object</code>
    * [.getAbsoluteRotation()](#Konva.Node+getAbsoluteRotation) ⇒ <code>Number</code>
    * [.getTransform()](#Konva.Node+getTransform) ⇒ [<code>Transform</code>](#Konva.Transform)
    * [.clone(obj)](#Konva.Node+clone) ⇒ [<code>Node</code>](#Konva.Node)
    * [.toCanvas(config)](#Konva.Node+toCanvas)
    * [.toDataURL(config)](#Konva.Node+toDataURL) ⇒ <code>String</code>
    * [.toImage(config)](#Konva.Node+toImage) ⇒ <code>Promise.&lt;Image&gt;</code>
    * [.toBlob(config)](#Konva.Node+toBlob) ⇒ <code>Promise.&lt;Blob&gt;</code>
    * [.getClassName()](#Konva.Node+getClassName) ⇒ <code>String</code>
    * [.getType()](#Konva.Node+getType) ⇒ <code>String</code>
    * [.addName(name)](#Konva.Node+addName) ⇒ [<code>Node</code>](#Konva.Node)
    * [.hasName(name)](#Konva.Node+hasName) ⇒ <code>Boolean</code>
    * [.removeName(name)](#Konva.Node+removeName) ⇒ [<code>Node</code>](#Konva.Node)
    * [.setAttr(attr, val)](#Konva.Node+setAttr) ⇒ [<code>Node</code>](#Konva.Node)
    * [.draw()](#Konva.Node+draw) ⇒ [<code>Node</code>](#Konva.Node)
    * [.startDrag()](#Konva.Node+startDrag)
    * [.stopDrag()](#Konva.Node+stopDrag)
    * [.isDragging()](#Konva.Node+isDragging)
    * [.isClientRectOnScreen(margin)](#Konva.Node+isClientRectOnScreen) ⇒ <code>Boolean</code>
    * [.zIndex(index)](#Konva.Node+zIndex) ⇒ <code>Number</code>
    * [.absolutePosition(pos)](#Konva.Node+absolutePosition) ⇒ <code>Object</code>
    * [.position(pos)](#Konva.Node+position) ⇒ <code>Object</code>
    * [.x(x)](#Konva.Node+x) ⇒ <code>Object</code>
    * [.y(y)](#Konva.Node+y) ⇒ <code>Integer</code>
    * [.globalCompositeOperation(type)](#Konva.Node+globalCompositeOperation) ⇒ <code>String</code>
    * [.opacity(opacity)](#Konva.Node+opacity) ⇒ <code>Number</code>
    * [.name(name)](#Konva.Node+name) ⇒ <code>String</code>
    * [.id(id)](#Konva.Node+id) ⇒ <code>String</code>
    * [.rotation(rotation)](#Konva.Node+rotation) ⇒ <code>Number</code>
    * [.scale(scale)](#Konva.Node+scale) ⇒ <code>Object</code>
    * [.scaleX(x)](#Konva.Node+scaleX) ⇒ <code>Number</code>
    * [.scaleY(y)](#Konva.Node+scaleY) ⇒ <code>Number</code>
    * [.skew(skew)](#Konva.Node+skew) ⇒ <code>Object</code>
    * [.skewX(x)](#Konva.Node+skewX) ⇒ <code>Number</code>
    * [.skewY(y)](#Konva.Node+skewY) ⇒ <code>Number</code>
    * [.offsetX(x)](#Konva.Node+offsetX) ⇒ <code>Number</code>
    * [.offsetY(y)](#Konva.Node+offsetY) ⇒ <code>Number</code>
    * [.dragDistance(distance)](#Konva.Node+dragDistance) ⇒ <code>Number</code>
    * [.width(width)](#Konva.Node+width) ⇒ <code>Number</code>
    * [.height(height)](#Konva.Node+height) ⇒ <code>Number</code>
    * [.listening(listening)](#Konva.Node+listening) ⇒ <code>Boolean</code>
    * [.preventDefault(preventDefault)](#Konva.Node+preventDefault) ⇒ <code>Boolean</code>
    * [.filters(filters)](#Konva.Node+filters) ⇒ <code>Array</code>
    * [.visible(visible)](#Konva.Node+visible) ⇒ <code>Boolean</code>
    * [.transformsEnabled(enabled)](#Konva.Node+transformsEnabled) ⇒ <code>String</code>
    * [.size(size)](#Konva.Node+size) ⇒ <code>Object</code>
    * [.dragBoundFunc(dragBoundFunc)](#Konva.Node+dragBoundFunc) ⇒ <code>function</code>
    * [.draggable(draggable)](#Konva.Node+draggable) ⇒ <code>Boolean</code>
    * [.to([params])](#Konva.Node+to)
    * [.blurRadius(radius)](#Konva.Node+blurRadius) ⇒ <code>Integer</code>
    * [.brightness(brightness)](#Konva.Node+brightness) ⇒ <code>Number</code>
    * [.contrast(contrast)](#Konva.Node+contrast) ⇒ <code>Number</code>
    * [.embossStrength(level)](#Konva.Node+embossStrength) ⇒ <code>Number</code>
    * [.embossWhiteLevel(embossWhiteLevel)](#Konva.Node+embossWhiteLevel) ⇒ <code>Number</code>
    * [.embossDirection(embossDirection)](#Konva.Node+embossDirection) ⇒ <code>String</code>
    * [.embossBlend(embossBlend)](#Konva.Node+embossBlend) ⇒ <code>Boolean</code>
    * [.enhance(amount)](#Konva.Node+enhance) ⇒ <code>Float</code>
    * [.hue(hue)](#Konva.Node+hue) ⇒ <code>Number</code>
    * [.saturation(saturation)](#Konva.Node+saturation) ⇒ <code>Number</code>
    * [.luminance(value)](#Konva.Node+luminance) ⇒ <code>Number</code>
    * [.value(value)](#Konva.Node+value) ⇒ <code>Number</code>
    * [.kaleidoscopePower(power)](#Konva.Node+kaleidoscopePower) ⇒ <code>Integer</code>
    * [.kaleidoscopeAngle(degrees)](#Konva.Node+kaleidoscopeAngle) ⇒ <code>Integer</code>
    * [.noise(noise)](#Konva.Node+noise) ⇒ <code>Number</code>
    * [.pixelSize(pixelSize)](#Konva.Node+pixelSize) ⇒ <code>Integer</code>
    * [.levels(level)](#Konva.Node+levels) ⇒ <code>Number</code>
    * [.red(red)](#Konva.Node+red) ⇒ <code>Integer</code>
    * [.green(green)](#Konva.Node+green) ⇒ <code>Integer</code>
    * [.blue(blue)](#Konva.Node+blue) ⇒ <code>Integer</code>
    * [.alpha(alpha)](#Konva.Node+alpha) ⇒ <code>Float</code>
    * [.threshold(threshold)](#Konva.Node+threshold) ⇒ <code>Number</code>

<a name="new_Konva.Circle_new"></a>

### new Circle(config)

| Param | Type | Description |
| --- | --- | --- |
| config | <code>Object</code> |  |
| config.radius | <code>Number</code> |  |
| [config.fill] | <code>String</code> | fill color |
| [config.fillPatternImage] | <code>Image</code> | fill pattern image |
| [config.fillPatternX] | <code>Number</code> |  |
| [config.fillPatternY] | <code>Number</code> |  |
| [config.fillPatternOffset] | <code>Object</code> | object with x and y component |
| [config.fillPatternOffsetX] | <code>Number</code> |  |
| [config.fillPatternOffsetY] | <code>Number</code> |  |
| [config.fillPatternScale] | <code>Object</code> | object with x and y component |
| [config.fillPatternScaleX] | <code>Number</code> |  |
| [config.fillPatternScaleY] | <code>Number</code> |  |
| [config.fillPatternRotation] | <code>Number</code> |  |
| [config.fillPatternRepeat] | <code>String</code> | can be "repeat", "repeat-x", "repeat-y", or "no-repeat".  The default is "no-repeat" |
| [config.fillLinearGradientStartPoint] | <code>Object</code> | object with x and y component |
| [config.fillLinearGradientStartPointX] | <code>Number</code> |  |
| [config.fillLinearGradientStartPointY] | <code>Number</code> |  |
| [config.fillLinearGradientEndPoint] | <code>Object</code> | object with x and y component |
| [config.fillLinearGradientEndPointX] | <code>Number</code> |  |
| [config.fillLinearGradientEndPointY] | <code>Number</code> |  |
| [config.fillLinearGradientColorStops] | <code>Array</code> | array of color stops |
| [config.fillRadialGradientStartPoint] | <code>Object</code> | object with x and y component |
| [config.fillRadialGradientStartPointX] | <code>Number</code> |  |
| [config.fillRadialGradientStartPointY] | <code>Number</code> |  |
| [config.fillRadialGradientEndPoint] | <code>Object</code> | object with x and y component |
| [config.fillRadialGradientEndPointX] | <code>Number</code> |  |
| [config.fillRadialGradientEndPointY] | <code>Number</code> |  |
| [config.fillRadialGradientStartRadius] | <code>Number</code> |  |
| [config.fillRadialGradientEndRadius] | <code>Number</code> |  |
| [config.fillRadialGradientColorStops] | <code>Array</code> | array of color stops |
| [config.fillEnabled] | <code>Boolean</code> | flag which enables or disables the fill.  The default value is true |
| [config.fillPriority] | <code>String</code> | can be color, linear-gradient, radial-graident, or pattern.  The default value is color.  The fillPriority property makes it really easy to toggle between different fill types.  For example, if you want to toggle between a fill color style and a fill pattern style, simply set the fill property and the fillPattern properties, and then use setFillPriority('color') to render the shape with a color fill, or use setFillPriority('pattern') to render the shape with the pattern fill configuration |
| [config.stroke] | <code>String</code> | stroke color |
| [config.strokeWidth] | <code>Number</code> | stroke width |
| [config.fillAfterStrokeEnabled] | <code>Boolean</code> | Should we draw fill AFTER stroke? Default is false. |
| [config.hitStrokeWidth] | <code>Number</code> | size of the stroke on hit canvas.  The default is "auto" - equals to strokeWidth |
| [config.strokeHitEnabled] | <code>Boolean</code> | flag which enables or disables stroke hit region.  The default is true |
| [config.perfectDrawEnabled] | <code>Boolean</code> | flag which enables or disables using buffer canvas.  The default is true |
| [config.shadowForStrokeEnabled] | <code>Boolean</code> | flag which enables or disables shadow for stroke.  The default is true |
| [config.strokeScaleEnabled] | <code>Boolean</code> | flag which enables or disables stroke scale.  The default is true |
| [config.strokeEnabled] | <code>Boolean</code> | flag which enables or disables the stroke.  The default value is true |
| [config.lineJoin] | <code>String</code> | can be miter, round, or bevel.  The default  is miter |
| [config.lineCap] | <code>String</code> | can be butt, round, or square.  The default  is butt |
| [config.shadowColor] | <code>String</code> |  |
| [config.shadowBlur] | <code>Number</code> |  |
| [config.shadowOffset] | <code>Object</code> | object with x and y component |
| [config.shadowOffsetX] | <code>Number</code> |  |
| [config.shadowOffsetY] | <code>Number</code> |  |
| [config.shadowOpacity] | <code>Number</code> | shadow opacity.  Can be any real number  between 0 and 1 |
| [config.shadowEnabled] | <code>Boolean</code> | flag which enables or disables the shadow.  The default value is true |
| [config.dash] | <code>Array</code> |  |
| [config.dashEnabled] | <code>Boolean</code> | flag which enables or disables the dashArray.  The default value is true |
| [config.x] | <code>Number</code> |  |
| [config.y] | <code>Number</code> |  |
| [config.width] | <code>Number</code> |  |
| [config.height] | <code>Number</code> |  |
| [config.visible] | <code>Boolean</code> |  |
| [config.listening] | <code>Boolean</code> | whether or not the node is listening for events |
| [config.id] | <code>String</code> | unique id |
| [config.name] | <code>String</code> | non-unique name |
| [config.opacity] | <code>Number</code> | determines node opacity.  Can be any number between 0 and 1 |
| [config.scale] | <code>Object</code> | set scale |
| [config.scaleX] | <code>Number</code> | set scale x |
| [config.scaleY] | <code>Number</code> | set scale y |
| [config.rotation] | <code>Number</code> | rotation in degrees |
| [config.offset] | <code>Object</code> | offset from center point and rotation point |
| [config.offsetX] | <code>Number</code> | set offset x |
| [config.offsetY] | <code>Number</code> | set offset y |
| [config.draggable] | <code>Boolean</code> | makes the node draggable.  When stages are draggable, you can drag and drop  the entire stage by dragging any portion of the stage |
| [config.dragDistance] | <code>Number</code> |  |
| [config.dragBoundFunc] | <code>function</code> |  |

**Example**  
```js
// create circle
var circle = new Konva.Circle({
  radius: 40,
  fill: 'red',
  stroke: 'black',
  strokeWidth: 5
});
```
<a name="Konva.Circle+radius"></a>

### circle.radius(radius) ⇒ <code>Number</code>
get/set radius


| Param | Type |
| --- | --- |
| radius | <code>Number</code> | 

**Example**  
```js
// get radius
var radius = circle.radius();

// set radius
circle.radius(10);
```
<a name="Konva.Shape+hasShadow"></a>

### circle.hasShadow() ⇒ <code>Boolean</code>
returns whether or not a shadow will be rendered

**Inherited from:** `Konva.Shape#hasShadow`

<a name="Konva.Shape+hasFill"></a>

### circle.hasFill() ⇒ <code>Boolean</code>
returns whether or not the shape will be filled

**Inherited from:** `Konva.Shape#hasFill`

<a name="Konva.Shape+hasStroke"></a>

### circle.hasStroke() ⇒ <code>Boolean</code>
returns whether or not the shape will be stroked

**Inherited from:** `Konva.Shape#hasStroke`

<a name="Konva.Shape+intersects"></a>

### circle.intersects(point) ⇒ <code>Boolean</code>
determines if point is in the shape, regardless if other shapes are on top of it.  Note: because
 this method clears a temporary canvas and then redraws the shape, it performs very poorly if executed many times
 consecutively.  Please use the [getIntersection](#Konva.Stage+getIntersection) method if at all possible
 because it performs much better

**Inherited from:** `Konva.Shape#intersects`


| Param | Type |
| --- | --- |
| point | <code>Object</code> | 
| point.x | <code>Number</code> | 
| point.y | <code>Number</code> | 

<a name="Konva.Shape+getSelfRect"></a>

### circle.getSelfRect() ⇒ <code>Object</code>
return self rectangle (x, y, width, height) of shape.
This method are not taken into account transformation and styles.

**Inherited from:** `Konva.Shape#getSelfRect`

**Returns**: <code>Object</code> - rect with `{x, y, width, height}` properties  
**Example**  
```js
rect.getSelfRect();  // return {x:0, y:0, width:rect.width(), height:rect.height()}
circle.getSelfRect();  // return {x: - circle.width() / 2, y: - circle.height() / 2, width:circle.width(), height:circle.height()}
```
<a name="Konva.Shape+drawHitFromCache"></a>

### circle.drawHitFromCache(alphaThreshold) ⇒ [<code>Shape</code>](#Konva.Shape)
draw hit graph using the cached scene canvas

**Inherited from:** `Konva.Shape#drawHitFromCache`


| Param | Type | Description |
| --- | --- | --- |
| alphaThreshold | <code>Integer</code> | alpha channel threshold that determines whether or not  a pixel should be drawn onto the hit graph.  Must be a value between 0 and 255.  The default is 0 |

**Example**  
```js
shape.cache();
shape.drawHitFromCache();
```
<a name="Konva.Shape+stroke"></a>

### circle.stroke(color) ⇒ <code>String</code>
get/set stroke color

**Inherited from:** `Konva.Shape#stroke`


| Param | Type |
| --- | --- |
| color | <code>String</code> | 

**Example**  
```js
// get stroke color
var stroke = shape.stroke();

// set stroke color with color string
shape.stroke('green');

// set stroke color with hex
shape.stroke('#00ff00');

// set stroke color with rgb
shape.stroke('rgb(0,255,0)');

// set stroke color with rgba and make it 50% opaque
shape.stroke('rgba(0,255,0,0.5');
```
<a name="Konva.Shape+strokeWidth"></a>

### circle.strokeWidth(strokeWidth) ⇒ <code>Number</code>
get/set stroke width

**Inherited from:** `Konva.Shape#strokeWidth`


| Param | Type |
| --- | --- |
| strokeWidth | <code>Number</code> | 

**Example**  
```js
// get stroke width
var strokeWidth = shape.strokeWidth();

// set stroke width
shape.strokeWidth(10);
```
<a name="Konva.Shape+fillAfterStrokeEnabled"></a>

### circle.fillAfterStrokeEnabled(fillAfterStrokeEnabled) ⇒ <code>Boolean</code>
get/set fillAfterStrokeEnabled property. By default Konva is drawing filling first, then stroke on top of the fill.
In rare situations you may want a different behavior. When you have a stroke first then fill on top of it.
Especially useful for Text objects.
Default is false.

**Inherited from:** `Konva.Shape#fillAfterStrokeEnabled`


| Param | Type |
| --- | --- |
| fillAfterStrokeEnabled | <code>Boolean</code> | 

**Example**  
```js
// get stroke width
var fillAfterStrokeEnabled = shape.fillAfterStrokeEnabled();

// set stroke width
shape.fillAfterStrokeEnabled(true);
```
<a name="Konva.Shape+hitStrokeWidth"></a>

### circle.hitStrokeWidth(hitStrokeWidth) ⇒ <code>Number</code>
get/set stroke width for hit detection. Default value is "auto", it means it will be equals to strokeWidth

**Inherited from:** `Konva.Shape#hitStrokeWidth`


| Param | Type |
| --- | --- |
| hitStrokeWidth | <code>Number</code> | 

**Example**  
```js
// get stroke width
var hitStrokeWidth = shape.hitStrokeWidth();

// set hit stroke width
shape.hitStrokeWidth(20);
// set hit stroke width always equals to scene stroke width
shape.hitStrokeWidth('auto');
```
<a name="Konva.Shape+strokeHitEnabled"></a>

### circle.strokeHitEnabled(strokeHitEnabled) ⇒ <code>Boolean</code>
**deprecated, use hitStrokeWidth instead!** get/set strokeHitEnabled property. Useful for performance optimization.
You may set `shape.strokeHitEnabled(false)`. In this case stroke will be no draw on hit canvas, so hit area
of shape will be decreased (by lineWidth / 2). Remember that non closed line with `strokeHitEnabled = false`
will be not drawn on hit canvas, that is mean line will no trigger pointer events (like mouseover)
Default value is true.

**Inherited from:** `Konva.Shape#strokeHitEnabled`


| Param | Type |
| --- | --- |
| strokeHitEnabled | <code>Boolean</code> | 

**Example**  
```js
// get strokeHitEnabled
var strokeHitEnabled = shape.strokeHitEnabled();

// set strokeHitEnabled
shape.strokeHitEnabled();
```
<a name="Konva.Shape+perfectDrawEnabled"></a>

### circle.perfectDrawEnabled(perfectDrawEnabled) ⇒ <code>Boolean</code>
get/set perfectDrawEnabled. If a shape has fill, stroke and opacity you may set `perfectDrawEnabled` to false to improve performance.
See http://konvajs.org/docs/performance/Disable_Perfect_Draw.html for more information.
Default value is true

**Inherited from:** `Konva.Shape#perfectDrawEnabled`


| Param | Type |
| --- | --- |
| perfectDrawEnabled | <code>Boolean</code> | 

**Example**  
```js
// get perfectDrawEnabled
var perfectDrawEnabled = shape.perfectDrawEnabled();

// set perfectDrawEnabled
shape.perfectDrawEnabled();
```
<a name="Konva.Shape+shadowForStrokeEnabled"></a>

### circle.shadowForStrokeEnabled(shadowForStrokeEnabled) ⇒ <code>Boolean</code>
get/set shadowForStrokeEnabled. Useful for performance optimization.
You may set `shape.shadowForStrokeEnabled(false)`. In this case stroke will no effect shadow.
Remember if you set `shadowForStrokeEnabled = false` for non closed line - that line will have no shadow!.
Default value is true

**Inherited from:** `Konva.Shape#shadowForStrokeEnabled`


| Param | Type |
| --- | --- |
| shadowForStrokeEnabled | <code>Boolean</code> | 

**Example**  
```js
// get shadowForStrokeEnabled
var shadowForStrokeEnabled = shape.shadowForStrokeEnabled();

// set shadowForStrokeEnabled
shape.shadowForStrokeEnabled();
```
<a name="Konva.Shape+lineJoin"></a>

### circle.lineJoin(lineJoin) ⇒ <code>String</code>
get/set line join.  Can be miter, round, or bevel.  The
 default is miter

**Inherited from:** `Konva.Shape#lineJoin`


| Param | Type |
| --- | --- |
| lineJoin | <code>String</code> | 

**Example**  
```js
// get line join
var lineJoin = shape.lineJoin();

// set line join
shape.lineJoin('round');
```
<a name="Konva.Shape+lineCap"></a>

### circle.lineCap(lineCap) ⇒ <code>String</code>
get/set line cap.  Can be butt, round, or square

**Inherited from:** `Konva.Shape#lineCap`


| Param | Type |
| --- | --- |
| lineCap | <code>String</code> | 

**Example**  
```js
// get line cap
var lineCap = shape.lineCap();

// set line cap
shape.lineCap('round');
```
<a name="Konva.Shape+sceneFunc"></a>

### circle.sceneFunc(drawFunc) ⇒ <code>function</code>
get/set scene draw function. That function is used to draw the shape on a canvas.
Also that function will be used to draw hit area of the shape, in case if hitFunc is not defined.

**Inherited from:** `Konva.Shape#sceneFunc`


| Param | Type | Description |
| --- | --- | --- |
| drawFunc | <code>function</code> | drawing function |

**Example**  
```js
// get scene draw function
var sceneFunc = shape.sceneFunc();

// set scene draw function
shape.sceneFunc(function(context, shape) {
  context.beginPath();
  context.rect(0, 0, shape.width(), shape.height());
  context.closePath();
  // important Konva method that fill and stroke shape from its properties
  // like stroke and fill
  context.fillStrokeShape(shape);
});
```
<a name="Konva.Shape+hitFunc"></a>

### circle.hitFunc(drawFunc) ⇒ <code>function</code>
get/set hit draw function. That function is used to draw custom hit area of a shape.

**Inherited from:** `Konva.Shape#hitFunc`


| Param | Type | Description |
| --- | --- | --- |
| drawFunc | <code>function</code> | drawing function |

**Example**  
```js
// get hit draw function
var hitFunc = shape.hitFunc();

// set hit draw function
shape.hitFunc(function(context) {
  context.beginPath();
  context.rect(0, 0, shape.width(), shape.height());
  context.closePath();
  // important Konva method that fill and stroke shape from its properties
  context.fillStrokeShape(shape);
});
```
<a name="Konva.Shape+dash"></a>

### circle.dash(dash) ⇒ <code>Array</code>
get/set dash array for stroke.

**Inherited from:** `Konva.Shape#dash`


| Param | Type |
| --- | --- |
| dash | <code>Array</code> | 

**Example**  
```js
// apply dashed stroke that is 10px long and 5 pixels apart
 line.dash([10, 5]);
 // apply dashed stroke that is made up of alternating dashed
 // lines that are 10px long and 20px apart, and dots that have
 // a radius of 5px and are 20px apart
 line.dash([10, 20, 0.001, 20]);
```
<a name="Konva.Shape+shadowColor"></a>

### circle.shadowColor(color) ⇒ <code>String</code>
get/set shadow color

**Inherited from:** `Konva.Shape#shadowColor`


| Param | Type |
| --- | --- |
| color | <code>String</code> | 

**Example**  
```js
// get shadow color
var shadow = shape.shadowColor();

// set shadow color with color string
shape.shadowColor('green');

// set shadow color with hex
shape.shadowColor('#00ff00');

// set shadow color with rgb
shape.shadowColor('rgb(0,255,0)');

// set shadow color with rgba and make it 50% opaque
shape.shadowColor('rgba(0,255,0,0.5');
```
<a name="Konva.Shape+shadowBlur"></a>

### circle.shadowBlur(blur) ⇒ <code>Number</code>
get/set shadow blur

**Inherited from:** `Konva.Shape#shadowBlur`


| Param | Type |
| --- | --- |
| blur | <code>Number</code> | 

**Example**  
```js
// get shadow blur
var shadowBlur = shape.shadowBlur();

// set shadow blur
shape.shadowBlur(10);
```
<a name="Konva.Shape+shadowOpacity"></a>

### circle.shadowOpacity(opacity) ⇒ <code>Number</code>
get/set shadow opacity.  must be a value between 0 and 1

**Inherited from:** `Konva.Shape#shadowOpacity`


| Param | Type |
| --- | --- |
| opacity | <code>Number</code> | 

**Example**  
```js
// get shadow opacity
var shadowOpacity = shape.shadowOpacity();

// set shadow opacity
shape.shadowOpacity(0.5);
```
<a name="Konva.Shape+shadowOffset"></a>

### circle.shadowOffset(offset) ⇒ <code>Object</code>
get/set shadow offset

**Inherited from:** `Konva.Shape#shadowOffset`


| Param | Type |
| --- | --- |
| offset | <code>Object</code> | 
| offset.x | <code>Number</code> | 
| offset.y | <code>Number</code> | 

**Example**  
```js
// get shadow offset
var shadowOffset = shape.shadowOffset();

// set shadow offset
shape.shadowOffset({
  x: 20,
  y: 10
});
```
<a name="Konva.Shape+shadowOffsetX"></a>

### circle.shadowOffsetX(x) ⇒ <code>Number</code>
get/set shadow offset x

**Inherited from:** `Konva.Shape#shadowOffsetX`


| Param | Type |
| --- | --- |
| x | <code>Number</code> | 

**Example**  
```js
// get shadow offset x
var shadowOffsetX = shape.shadowOffsetX();

// set shadow offset x
shape.shadowOffsetX(5);
```
<a name="Konva.Shape+shadowOffsetY"></a>

### circle.shadowOffsetY(y) ⇒ <code>Number</code>
get/set shadow offset y

**Inherited from:** `Konva.Shape#shadowOffsetY`


| Param | Type |
| --- | --- |
| y | <code>Number</code> | 

**Example**  
```js
// get shadow offset y
var shadowOffsetY = shape.shadowOffsetY();

// set shadow offset y
shape.shadowOffsetY(5);
```
<a name="Konva.Shape+fillPatternImage"></a>

### circle.fillPatternImage(image) ⇒ <code>Image</code>
get/set fill pattern image

**Inherited from:** `Konva.Shape#fillPatternImage`


| Param | Type | Description |
| --- | --- | --- |
| image | <code>Image</code> | object |

**Example**  
```js
// get fill pattern image
var fillPatternImage = shape.fillPatternImage();

// set fill pattern image
var imageObj = new Image();
imageObj.onload = function() {
  shape.fillPatternImage(imageObj);
};
imageObj.src = 'path/to/image/jpg';
```
<a name="Konva.Shape+fill"></a>

### circle.fill(color) ⇒ <code>String</code>
get/set fill color

**Inherited from:** `Konva.Shape#fill`


| Param | Type |
| --- | --- |
| color | <code>String</code> | 

**Example**  
```js
// get fill color
var fill = shape.fill();

// set fill color with color string
shape.fill('green');

// set fill color with hex
shape.fill('#00ff00');

// set fill color with rgb
shape.fill('rgb(0,255,0)');

// set fill color with rgba and make it 50% opaque
shape.fill('rgba(0,255,0,0.5');

// shape without fill
shape.fill(null);
```
<a name="Konva.Shape+fillPatternX"></a>

### circle.fillPatternX(x) ⇒ <code>Number</code>
get/set fill pattern x

**Inherited from:** `Konva.Shape#fillPatternX`


| Param | Type |
| --- | --- |
| x | <code>Number</code> | 

**Example**  
```js
// get fill pattern x
var fillPatternX = shape.fillPatternX();
// set fill pattern x
shape.fillPatternX(20);
```
<a name="Konva.Shape+fillPatternY"></a>

### circle.fillPatternY(y) ⇒ <code>Number</code>
get/set fill pattern y

**Inherited from:** `Konva.Shape#fillPatternY`


| Param | Type |
| --- | --- |
| y | <code>Number</code> | 

**Example**  
```js
// get fill pattern y
var fillPatternY = shape.fillPatternY();
// set fill pattern y
shape.fillPatternY(20);
```
<a name="Konva.Shape+fillLinearGradientColorStops"></a>

### circle.fillLinearGradientColorStops(colorStops) ⇒ <code>Array</code>
get/set fill linear gradient color stops

**Inherited from:** `Konva.Shape#fillLinearGradientColorStops`

**Returns**: <code>Array</code> - colorStops  

| Param | Type |
| --- | --- |
| colorStops | <code>Array</code> | 

**Example**  
```js
// get fill linear gradient color stops
var colorStops = shape.fillLinearGradientColorStops();

// create a linear gradient that starts with red, changes to blue
// halfway through, and then changes to green
shape.fillLinearGradientColorStops(0, 'red', 0.5, 'blue', 1, 'green');
```
<a name="Konva.Shape+strokeLinearGradientColorStops"></a>

### circle.strokeLinearGradientColorStops(colorStops) ⇒ <code>Array</code>
get/set stroke linear gradient color stops

**Inherited from:** `Konva.Shape#strokeLinearGradientColorStops`

**Returns**: <code>Array</code> - colorStops  

| Param | Type |
| --- | --- |
| colorStops | <code>Array</code> | 

**Example**  
```js
// get stroke linear gradient color stops
var colorStops = shape.strokeLinearGradientColorStops();

// create a linear gradient that starts with red, changes to blue
// halfway through, and then changes to green
shape.strokeLinearGradientColorStops([0, 'red', 0.5, 'blue', 1, 'green']);
```
<a name="Konva.Shape+fillRadialGradientStartRadius"></a>

### circle.fillRadialGradientStartRadius(radius) ⇒ <code>Number</code>
get/set fill radial gradient start radius

**Inherited from:** `Konva.Shape#fillRadialGradientStartRadius`


| Param | Type |
| --- | --- |
| radius | <code>Number</code> | 

**Example**  
```js
// get radial gradient start radius
var startRadius = shape.fillRadialGradientStartRadius();

// set radial gradient start radius
shape.fillRadialGradientStartRadius(0);
```
<a name="Konva.Shape+fillRadialGradientEndRadius"></a>

### circle.fillRadialGradientEndRadius(radius) ⇒ <code>Number</code>
get/set fill radial gradient end radius

**Inherited from:** `Konva.Shape#fillRadialGradientEndRadius`


| Param | Type |
| --- | --- |
| radius | <code>Number</code> | 

**Example**  
```js
// get radial gradient end radius
var endRadius = shape.fillRadialGradientEndRadius();

// set radial gradient end radius
shape.fillRadialGradientEndRadius(100);
```
<a name="Konva.Shape+fillRadialGradientColorStops"></a>

### circle.fillRadialGradientColorStops(colorStops) ⇒ <code>Array</code>
get/set fill radial gradient color stops

**Inherited from:** `Konva.Shape#fillRadialGradientColorStops`


| Param | Type |
| --- | --- |
| colorStops | <code>Number</code> | 

**Example**  
```js
// get fill radial gradient color stops
var colorStops = shape.fillRadialGradientColorStops();

// create a radial gradient that starts with red, changes to blue
// halfway through, and then changes to green
shape.fillRadialGradientColorStops(0, 'red', 0.5, 'blue', 1, 'green');
```
<a name="Konva.Shape+fillPatternRepeat"></a>

### circle.fillPatternRepeat(repeat) ⇒ <code>String</code>
get/set fill pattern repeat.  Can be 'repeat', 'repeat-x', 'repeat-y', or 'no-repeat'.  The default is 'repeat'

**Inherited from:** `Konva.Shape#fillPatternRepeat`


| Param | Type |
| --- | --- |
| repeat | <code>String</code> | 

**Example**  
```js
// get fill pattern repeat
var repeat = shape.fillPatternRepeat();

// repeat pattern in x direction only
shape.fillPatternRepeat('repeat-x');

// do not repeat the pattern
shape.fillPatternRepeat('no-repeat');
```
<a name="Konva.Shape+fillEnabled"></a>

### circle.fillEnabled(enabled) ⇒ <code>Boolean</code>
get/set fill enabled flag

**Inherited from:** `Konva.Shape#fillEnabled`


| Param | Type |
| --- | --- |
| enabled | <code>Boolean</code> | 

**Example**  
```js
// get fill enabled flag
var fillEnabled = shape.fillEnabled();

// disable fill
shape.fillEnabled(false);

// enable fill
shape.fillEnabled(true);
```
<a name="Konva.Shape+strokeEnabled"></a>

### circle.strokeEnabled(enabled) ⇒ <code>Boolean</code>
get/set stroke enabled flag

**Inherited from:** `Konva.Shape#strokeEnabled`


| Param | Type |
| --- | --- |
| enabled | <code>Boolean</code> | 

**Example**  
```js
// get stroke enabled flag
var strokeEnabled = shape.strokeEnabled();

// disable stroke
shape.strokeEnabled(false);

// enable stroke
shape.strokeEnabled(true);
```
<a name="Konva.Shape+shadowEnabled"></a>

### circle.shadowEnabled(enabled) ⇒ <code>Boolean</code>
get/set shadow enabled flag

**Inherited from:** `Konva.Shape#shadowEnabled`


| Param | Type |
| --- | --- |
| enabled | <code>Boolean</code> | 

**Example**  
```js
// get shadow enabled flag
var shadowEnabled = shape.shadowEnabled();

// disable shadow
shape.shadowEnabled(false);

// enable shadow
shape.shadowEnabled(true);
```
<a name="Konva.Shape+dashEnabled"></a>

### circle.dashEnabled(enabled) ⇒ <code>Boolean</code>
get/set dash enabled flag

**Inherited from:** `Konva.Shape#dashEnabled`


| Param | Type |
| --- | --- |
| enabled | <code>Boolean</code> | 

**Example**  
```js
// get dash enabled flag
var dashEnabled = shape.dashEnabled();

// disable dash
shape.dashEnabled(false);

// enable dash
shape.dashEnabled(true);
```
<a name="Konva.Shape+strokeScaleEnabled"></a>

### circle.strokeScaleEnabled(enabled) ⇒ <code>Boolean</code>
get/set strokeScale enabled flag

**Inherited from:** `Konva.Shape#strokeScaleEnabled`


| Param | Type |
| --- | --- |
| enabled | <code>Boolean</code> | 

**Example**  
```js
// get stroke scale enabled flag
var strokeScaleEnabled = shape.strokeScaleEnabled();

// disable stroke scale
shape.strokeScaleEnabled(false);

// enable stroke scale
shape.strokeScaleEnabled(true);
```
<a name="Konva.Shape+fillPriority"></a>

### circle.fillPriority(priority) ⇒ <code>String</code>
get/set fill priority.  can be color, pattern, linear-gradient, or radial-gradient.  The default is color.
  This is handy if you want to toggle between different fill types.

**Inherited from:** `Konva.Shape#fillPriority`


| Param | Type |
| --- | --- |
| priority | <code>String</code> | 

**Example**  
```js
// get fill priority
var fillPriority = shape.fillPriority();

// set fill priority
shape.fillPriority('linear-gradient');
```
<a name="Konva.Shape+fillPatternOffset"></a>

### circle.fillPatternOffset(offset) ⇒ <code>Object</code>
get/set fill pattern offset

**Inherited from:** `Konva.Shape#fillPatternOffset`


| Param | Type |
| --- | --- |
| offset | <code>Object</code> | 
| offset.x | <code>Number</code> | 
| offset.y | <code>Number</code> | 

**Example**  
```js
// get fill pattern offset
var patternOffset = shape.fillPatternOffset();

// set fill pattern offset
shape.fillPatternOffset({
  x: 20,
  y: 10
});
```
<a name="Konva.Shape+fillPatternOffsetX"></a>

### circle.fillPatternOffsetX(x) ⇒ <code>Number</code>
get/set fill pattern offset x

**Inherited from:** `Konva.Shape#fillPatternOffsetX`


| Param | Type |
| --- | --- |
| x | <code>Number</code> | 

**Example**  
```js
// get fill pattern offset x
var patternOffsetX = shape.fillPatternOffsetX();

// set fill pattern offset x
shape.fillPatternOffsetX(20);
```
<a name="Konva.Shape+fillPatternOffsetY"></a>

### circle.fillPatternOffsetY(y) ⇒ <code>Number</code>
get/set fill pattern offset y

**Inherited from:** `Konva.Shape#fillPatternOffsetY`


| Param | Type |
| --- | --- |
| y | <code>Number</code> | 

**Example**  
```js
// get fill pattern offset y
var patternOffsetY = shape.fillPatternOffsetY();

// set fill pattern offset y
shape.fillPatternOffsetY(10);
```
<a name="Konva.Shape+fillPatternScale"></a>

### circle.fillPatternScale(scale) ⇒ <code>Object</code>
get/set fill pattern scale

**Inherited from:** `Konva.Shape#fillPatternScale`


| Param | Type |
| --- | --- |
| scale | <code>Object</code> | 
| scale.x | <code>Number</code> | 
| scale.y | <code>Number</code> | 

**Example**  
```js
// get fill pattern scale
var patternScale = shape.fillPatternScale();

// set fill pattern scale
shape.fillPatternScale({
  x: 2,
  y: 2
});
```
<a name="Konva.Shape+fillPatternScaleX"></a>

### circle.fillPatternScaleX(x) ⇒ <code>Number</code>
get/set fill pattern scale x

**Inherited from:** `Konva.Shape#fillPatternScaleX`


| Param | Type |
| --- | --- |
| x | <code>Number</code> | 

**Example**  
```js
// get fill pattern scale x
var patternScaleX = shape.fillPatternScaleX();

// set fill pattern scale x
shape.fillPatternScaleX(2);
```
<a name="Konva.Shape+fillPatternScaleY"></a>

### circle.fillPatternScaleY(y) ⇒ <code>Number</code>
get/set fill pattern scale y

**Inherited from:** `Konva.Shape#fillPatternScaleY`


| Param | Type |
| --- | --- |
| y | <code>Number</code> | 

**Example**  
```js
// get fill pattern scale y
var patternScaleY = shape.fillPatternScaleY();

// set fill pattern scale y
shape.fillPatternScaleY(2);
```
<a name="Konva.Shape+fillLinearGradientStartPoint"></a>

### circle.fillLinearGradientStartPoint(startPoint) ⇒ <code>Object</code>
get/set fill linear gradient start point

**Inherited from:** `Konva.Shape#fillLinearGradientStartPoint`


| Param | Type |
| --- | --- |
| startPoint | <code>Object</code> | 
| startPoint.x | <code>Number</code> | 
| startPoint.y | <code>Number</code> | 

**Example**  
```js
// get fill linear gradient start point
var startPoint = shape.fillLinearGradientStartPoint();

// set fill linear gradient start point
shape.fillLinearGradientStartPoint({
  x: 20,
  y: 10
});
```
<a name="Konva.Shape+strokeLinearGradientStartPoint"></a>

### circle.strokeLinearGradientStartPoint(startPoint) ⇒ <code>Object</code>
get/set stroke linear gradient start point

**Inherited from:** `Konva.Shape#strokeLinearGradientStartPoint`


| Param | Type |
| --- | --- |
| startPoint | <code>Object</code> | 
| startPoint.x | <code>Number</code> | 
| startPoint.y | <code>Number</code> | 

**Example**  
```js
// get stroke linear gradient start point
var startPoint = shape.strokeLinearGradientStartPoint();

// set stroke linear gradient start point
shape.strokeLinearGradientStartPoint({
  x: 20,
  y: 10
});
```
<a name="Konva.Shape+fillLinearGradientStartPointX"></a>

### circle.fillLinearGradientStartPointX(x) ⇒ <code>Number</code>
get/set fill linear gradient start point x

**Inherited from:** `Konva.Shape#fillLinearGradientStartPointX`


| Param | Type |
| --- | --- |
| x | <code>Number</code> | 

**Example**  
```js
// get fill linear gradient start point x
var startPointX = shape.fillLinearGradientStartPointX();

// set fill linear gradient start point x
shape.fillLinearGradientStartPointX(20);
```
<a name="Konva.Shape+linearLinearGradientStartPointX"></a>

### circle.linearLinearGradientStartPointX(x) ⇒ <code>Number</code>
get/set stroke linear gradient start point x

**Inherited from:** `Konva.Shape#linearLinearGradientStartPointX`


| Param | Type |
| --- | --- |
| x | <code>Number</code> | 

**Example**  
```js
// get stroke linear gradient start point x
var startPointX = shape.strokeLinearGradientStartPointX();

// set stroke linear gradient start point x
shape.strokeLinearGradientStartPointX(20);
```
<a name="Konva.Shape+fillLinearGradientStartPointY"></a>

### circle.fillLinearGradientStartPointY(y) ⇒ <code>Number</code>
get/set fill linear gradient start point y

**Inherited from:** `Konva.Shape#fillLinearGradientStartPointY`


| Param | Type |
| --- | --- |
| y | <code>Number</code> | 

**Example**  
```js
// get fill linear gradient start point y
var startPointY = shape.fillLinearGradientStartPointY();

// set fill linear gradient start point y
shape.fillLinearGradientStartPointY(20);
```
<a name="Konva.Shape+strokeLinearGradientStartPointY"></a>

### circle.strokeLinearGradientStartPointY(y) ⇒ <code>Number</code>
get/set stroke linear gradient start point y

**Inherited from:** `Konva.Shape#strokeLinearGradientStartPointY`


| Param | Type |
| --- | --- |
| y | <code>Number</code> | 

**Example**  
```js
// get stroke linear gradient start point y
var startPointY = shape.strokeLinearGradientStartPointY();

// set stroke linear gradient start point y
shape.strokeLinearGradientStartPointY(20);
```
<a name="Konva.Shape+fillLinearGradientEndPoint"></a>

### circle.fillLinearGradientEndPoint(endPoint) ⇒ <code>Object</code>
get/set fill linear gradient end point

**Inherited from:** `Konva.Shape#fillLinearGradientEndPoint`


| Param | Type |
| --- | --- |
| endPoint | <code>Object</code> | 
| endPoint.x | <code>Number</code> | 
| endPoint.y | <code>Number</code> | 

**Example**  
```js
// get fill linear gradient end point
var endPoint = shape.fillLinearGradientEndPoint();

// set fill linear gradient end point
shape.fillLinearGradientEndPoint({
  x: 20,
  y: 10
});
```
<a name="Konva.Shape+strokeLinearGradientEndPoint"></a>

### circle.strokeLinearGradientEndPoint(endPoint) ⇒ <code>Object</code>
get/set stroke linear gradient end point

**Inherited from:** `Konva.Shape#strokeLinearGradientEndPoint`


| Param | Type |
| --- | --- |
| endPoint | <code>Object</code> | 
| endPoint.x | <code>Number</code> | 
| endPoint.y | <code>Number</code> | 

**Example**  
```js
// get stroke linear gradient end point
var endPoint = shape.strokeLinearGradientEndPoint();

// set stroke linear gradient end point
shape.strokeLinearGradientEndPoint({
  x: 20,
  y: 10
});
```
<a name="Konva.Shape+fillLinearGradientEndPointX"></a>

### circle.fillLinearGradientEndPointX(x) ⇒ <code>Number</code>
get/set fill linear gradient end point x

**Inherited from:** `Konva.Shape#fillLinearGradientEndPointX`


| Param | Type |
| --- | --- |
| x | <code>Number</code> | 

**Example**  
```js
// get fill linear gradient end point x
var endPointX = shape.fillLinearGradientEndPointX();

// set fill linear gradient end point x
shape.fillLinearGradientEndPointX(20);
```
<a name="Konva.Shape+strokeLinearGradientEndPointX"></a>

### circle.strokeLinearGradientEndPointX(x) ⇒ <code>Number</code>
get/set fill linear gradient end point x

**Inherited from:** `Konva.Shape#strokeLinearGradientEndPointX`


| Param | Type |
| --- | --- |
| x | <code>Number</code> | 

**Example**  
```js
// get stroke linear gradient end point x
var endPointX = shape.strokeLinearGradientEndPointX();

// set stroke linear gradient end point x
shape.strokeLinearGradientEndPointX(20);
```
<a name="Konva.Shape+fillLinearGradientEndPointY"></a>

### circle.fillLinearGradientEndPointY(y) ⇒ <code>Number</code>
get/set fill linear gradient end point y

**Inherited from:** `Konva.Shape#fillLinearGradientEndPointY`


| Param | Type |
| --- | --- |
| y | <code>Number</code> | 

**Example**  
```js
// get fill linear gradient end point y
var endPointY = shape.fillLinearGradientEndPointY();

// set fill linear gradient end point y
shape.fillLinearGradientEndPointY(20);
```
<a name="Konva.Shape+strokeLinearGradientEndPointY"></a>

### circle.strokeLinearGradientEndPointY(y) ⇒ <code>Number</code>
get/set stroke linear gradient end point y

**Inherited from:** `Konva.Shape#strokeLinearGradientEndPointY`


| Param | Type |
| --- | --- |
| y | <code>Number</code> | 

**Example**  
```js
// get stroke linear gradient end point y
var endPointY = shape.strokeLinearGradientEndPointY();

// set stroke linear gradient end point y
shape.strokeLinearGradientEndPointY(20);
```
<a name="Konva.Shape+fillRadialGradientStartPoint"></a>

### circle.fillRadialGradientStartPoint(startPoint) ⇒ <code>Object</code>
get/set fill radial gradient start point

**Inherited from:** `Konva.Shape#fillRadialGradientStartPoint`


| Param | Type |
| --- | --- |
| startPoint | <code>Object</code> | 
| startPoint.x | <code>Number</code> | 
| startPoint.y | <code>Number</code> | 

**Example**  
```js
// get fill radial gradient start point
var startPoint = shape.fillRadialGradientStartPoint();

// set fill radial gradient start point
shape.fillRadialGradientStartPoint({
  x: 20,
  y: 10
});
```
<a name="Konva.Shape+fillRadialGradientStartPointX"></a>

### circle.fillRadialGradientStartPointX(x) ⇒ <code>Number</code>
get/set fill radial gradient start point x

**Inherited from:** `Konva.Shape#fillRadialGradientStartPointX`


| Param | Type |
| --- | --- |
| x | <code>Number</code> | 

**Example**  
```js
// get fill radial gradient start point x
var startPointX = shape.fillRadialGradientStartPointX();

// set fill radial gradient start point x
shape.fillRadialGradientStartPointX(20);
```
<a name="Konva.Shape+fillRadialGradientStartPointY"></a>

### circle.fillRadialGradientStartPointY(y) ⇒ <code>Number</code>
get/set fill radial gradient start point y

**Inherited from:** `Konva.Shape#fillRadialGradientStartPointY`


| Param | Type |
| --- | --- |
| y | <code>Number</code> | 

**Example**  
```js
// get fill radial gradient start point y
var startPointY = shape.fillRadialGradientStartPointY();

// set fill radial gradient start point y
shape.fillRadialGradientStartPointY(20);
```
<a name="Konva.Shape+fillRadialGradientEndPoint"></a>

### circle.fillRadialGradientEndPoint(endPoint) ⇒ <code>Object</code>
get/set fill radial gradient end point

**Inherited from:** `Konva.Shape#fillRadialGradientEndPoint`


| Param | Type |
| --- | --- |
| endPoint | <code>Object</code> | 
| endPoint.x | <code>Number</code> | 
| endPoint.y | <code>Number</code> | 

**Example**  
```js
// get fill radial gradient end point
var endPoint = shape.fillRadialGradientEndPoint();

// set fill radial gradient end point
shape.fillRadialGradientEndPoint({
  x: 20,
  y: 10
});
```
<a name="Konva.Shape+fillRadialGradientEndPointX"></a>

### circle.fillRadialGradientEndPointX(x) ⇒ <code>Number</code>
get/set fill radial gradient end point x

**Inherited from:** `Konva.Shape#fillRadialGradientEndPointX`


| Param | Type |
| --- | --- |
| x | <code>Number</code> | 

**Example**  
```js
// get fill radial gradient end point x
var endPointX = shape.fillRadialGradientEndPointX();

// set fill radial gradient end point x
shape.fillRadialGradientEndPointX(20);
```
<a name="Konva.Shape+fillRadialGradientEndPointY"></a>

### circle.fillRadialGradientEndPointY(y) ⇒ <code>Number</code>
get/set fill radial gradient end point y

**Inherited from:** `Konva.Shape#fillRadialGradientEndPointY`


| Param | Type |
| --- | --- |
| y | <code>Number</code> | 

**Example**  
```js
// get fill radial gradient end point y
var endPointY = shape.fillRadialGradientEndPointY();

// set fill radial gradient end point y
shape.fillRadialGradientEndPointY(20);
```
<a name="Konva.Shape+fillPatternRotation"></a>

### circle.fillPatternRotation(rotation) ⇒ [<code>Shape</code>](#Konva.Shape)
get/set fill pattern rotation in degrees

**Inherited from:** `Konva.Shape#fillPatternRotation`


| Param | Type |
| --- | --- |
| rotation | <code>Number</code> | 

**Example**  
```js
// get fill pattern rotation
var patternRotation = shape.fillPatternRotation();

// set fill pattern rotation
shape.fillPatternRotation(20);
```
<a name="Konva.Shape+fillRule"></a>

### circle.fillRule(rotation) ⇒ [<code>Shape</code>](#Konva.Shape)
get/set fill rule

**Inherited from:** `Konva.Shape#fillRule`


| Param | Type |
| --- | --- |
| rotation | <code>CanvasFillRule</code> | 

**Example**  
```js
// get fill rule
var fillRule = shape.fillRule();

// set fill rule
shape.fillRule('evenodd');
```
<a name="Konva.Node+clearCache"></a>

### circle.clearCache() ⇒ [<code>Node</code>](#Konva.Node)
clear cached canvas

**Inherited from:** `Konva.Node#clearCache`

**Example**  
```js
node.clearCache();
```
<a name="Konva.Node+cache"></a>

### circle.cache([config]) ⇒ [<code>Node</code>](#Konva.Node)
cache node to improve drawing performance, apply filters, or create more accurate
 hit regions. For all basic shapes size of cache canvas will be automatically detected.
 If you need to cache your custom `Konva.Shape` instance you have to pass shape's bounding box
 properties. Look at [https://konvajs.org/docs/performance/Shape_Caching.html](https://konvajs.org/docs/performance/Shape_Caching.html) for more information.

**Inherited from:** `Konva.Node#cache`


| Param | Type | Description |
| --- | --- | --- |
| [config] | <code>Object</code> |  |
| [config.x] | <code>Number</code> |  |
| [config.y] | <code>Number</code> |  |
| [config.width] | <code>Number</code> |  |
| [config.height] | <code>Number</code> |  |
| [config.offset] | <code>Number</code> | increase canvas size by `offset` pixel in all directions. |
| [config.drawBorder] | <code>Boolean</code> | when set to true, a red border will be drawn around the cached  region for debugging purposes |
| [config.pixelRatio] | <code>Number</code> | change quality (or pixel ratio) of cached image. pixelRatio = 2 will produce 2x sized cache. |
| [config.imageSmoothingEnabled] | <code>Boolean</code> | control imageSmoothingEnabled property of created canvas for cache |
| [config.hitCanvasPixelRatio] | <code>Number</code> | change quality (or pixel ratio) of cached hit canvas. |

**Example**  
```js
// cache a shape with the x,y position of the bounding box at the center and
// the width and height of the bounding box equal to the width and height of
// the shape obtained from shape.width() and shape.height()
image.cache();

// cache a node and define the bounding box position and size
node.cache({
  x: -30,
  y: -30,
  width: 100,
  height: 200
});

// cache a node and draw a red border around the bounding box
// for debugging purposes
node.cache({
  x: -30,
  y: -30,
  width: 100,
  height: 200,
  offset : 10,
  drawBorder: true
});
```
<a name="Konva.Node+isCached"></a>

### circle.isCached() ⇒ <code>Boolean</code>
determine if node is currently cached

**Inherited from:** `Konva.Node#isCached`

<a name="Konva.Node+getClientRect"></a>

### circle.getClientRect(config) ⇒ <code>Object</code>
Return client rectangle `{x, y, width, height}` of node. This rectangle also include all styling (strokes, shadows, etc).
The purpose of the method is similar to getBoundingClientRect API of the DOM.

**Inherited from:** `Konva.Node#getClientRect`

**Returns**: <code>Object</code> - rect with `{x, y, width, height}` properties  

| Param | Type | Description |
| --- | --- | --- |
| config | <code>Object</code> |  |
| [config.skipTransform] | <code>Boolean</code> | should we apply transform to node for calculating rect? |
| [config.skipShadow] | <code>Boolean</code> | should we apply shadow to the node for calculating bound box? |
| [config.skipStroke] | <code>Boolean</code> | should we apply stroke to the node for calculating bound box? |
| [config.relativeTo] | <code>Object</code> | calculate client rect relative to one of the parents |

**Example**  
```js
var rect = new Konva.Rect({
     width : 100,
     height : 100,
     x : 50,
     y : 50,
     strokeWidth : 4,
     stroke : 'black',
     offsetX : 50,
     scaleY : 2
});

// get client rect without think off transformations (position, rotation, scale, offset, etc)
rect.getClientRect({ skipTransform: true});
// returns {
//     x : -2,   // two pixels for stroke / 2
//     y : -2,
//     width : 104, // increased by 4 for stroke
//     height : 104
//}

// get client rect with transformation applied
rect.getClientRect();
// returns Object {x: -2, y: 46, width: 104, height: 208}
```
<a name="Konva.Node+on"></a>

### circle.on(evtStr, handler) ⇒ [<code>Node</code>](#Konva.Node)
bind events to the node. KonvaJS supports mouseover, mousemove,
 mouseout, mouseenter, mouseleave, mousedown, mouseup, wheel, contextmenu, click, dblclick, touchstart, touchmove,
 touchend, tap, dbltap, dragstart, dragmove, and dragend events.
 Pass in a string of events delimited by a space to bind multiple events at once
 such as 'mousedown mouseup mousemove'. Include a namespace to bind an
 event by name such as 'click.foobar'.

**Inherited from:** `Konva.Node#on`


| Param | Type | Description |
| --- | --- | --- |
| evtStr | <code>String</code> | e.g. 'click', 'mousedown touchstart', 'mousedown.foo touchstart.foo' |
| handler | <code>function</code> | The handler function. The first argument of that function is event object. Event object has `target` as main target of the event, `currentTarget` as current node listener and `evt` as native browser event. |

**Example**  
```js
// add click listener
node.on('click', function() {
  console.log('you clicked me!');
});

// get the target node
node.on('click', function(evt) {
  console.log(evt.target);
});

// stop event propagation
node.on('click', function(evt) {
  evt.cancelBubble = true;
});

// bind multiple listeners
node.on('click touchstart', function() {
  console.log('you clicked/touched me!');
});

// namespace listener
node.on('click.foo', function() {
  console.log('you clicked/touched me!');
});

// get the event type
node.on('click tap', function(evt) {
  var eventType = evt.type;
});

// get native event object
node.on('click tap', function(evt) {
  var nativeEvent = evt.evt;
});

// for change events, get the old and new val
node.on('xChange', function(evt) {
  var oldVal = evt.oldVal;
  var newVal = evt.newVal;
});

// get event targets
// with event delegations
layer.on('click', 'Group', function(evt) {
  var shape = evt.target;
  var group = evt.currentTarget;
});
```
<a name="Konva.Node+off"></a>

### circle.off(evtStr) ⇒ [<code>Node</code>](#Konva.Node)
remove event bindings from the node. Pass in a string of
 event types delimmited by a space to remove multiple event
 bindings at once such as 'mousedown mouseup mousemove'.
 include a namespace to remove an event binding by name
 such as 'click.foobar'. If you only give a name like '.foobar',
 all events in that namespace will be removed.

**Inherited from:** `Konva.Node#off`


| Param | Type | Description |
| --- | --- | --- |
| evtStr | <code>String</code> | e.g. 'click', 'mousedown touchstart', '.foobar' |

**Example**  
```js
// remove listener
node.off('click');

// remove multiple listeners
node.off('click touchstart');

// remove listener by name
node.off('click.foo');
```
<a name="Konva.Node+remove"></a>

### circle.remove() ⇒ [<code>Node</code>](#Konva.Node)
remove a node from parent, but don't destroy. You can reuse the node later.

**Inherited from:** `Konva.Node#remove`

**Example**  
```js
node.remove();
```
<a name="Konva.Node+destroy"></a>

### circle.destroy()
remove and destroy a node. Kill it and delete forever! You should not reuse node after destroy().
If the node is a container (Group, Stage or Layer) it will destroy all children too.

**Inherited from:** `Konva.Node#destroy`

**Example**  
```js
node.destroy();
```
<a name="Konva.Node+getAttr"></a>

### circle.getAttr(attr) ⇒ <code>Integer</code> \| <code>String</code> \| <code>Object</code> \| <code>Array</code>
get attr

**Inherited from:** `Konva.Node#getAttr`


| Param | Type |
| --- | --- |
| attr | <code>String</code> | 

**Example**  
```js
var x = node.getAttr('x');
```
<a name="Konva.Node+getAncestors"></a>

### circle.getAncestors() ⇒ <code>Array</code>
get ancestors

**Inherited from:** `Konva.Node#getAncestors`

**Example**  
```js
shape.getAncestors().forEach(function(node) {
  console.log(node.getId());
})
```
<a name="Konva.Node+getAttrs"></a>

### circle.getAttrs() ⇒ <code>Object</code>
get attrs object literal

**Inherited from:** `Konva.Node#getAttrs`

<a name="Konva.Node+setAttrs"></a>

### circle.setAttrs(config) ⇒ [<code>Node</code>](#Konva.Node)
set multiple attrs at once using an object literal

**Inherited from:** `Konva.Node#setAttrs`


| Param | Type | Description |
| --- | --- | --- |
| config | <code>Object</code> | object containing key value pairs |

**Example**  
```js
node.setAttrs({
  x: 5,
  fill: 'red'
});
```
<a name="Konva.Node+isListening"></a>

### circle.isListening() ⇒ <code>Boolean</code>
determine if node is listening for events by taking into account ancestors.

Parent    | Self      | isListening
listening | listening |
----------+-----------+------------
T         | T         | T
T         | F         | F
F         | T         | F
F         | F         | F

**Inherited from:** `Konva.Node#isListening`

<a name="Konva.Node+isVisible"></a>

### circle.isVisible() ⇒ <code>Boolean</code>
determine if node is visible by taking into account ancestors.

Parent    | Self      | isVisible
visible   | visible   |
----------+-----------+------------
T         | T         | T
T         | F         | F
F         | T         | F
F         | F         | F

**Inherited from:** `Konva.Node#isVisible`

<a name="Konva.Node+show"></a>

### circle.show() ⇒ [<code>Node</code>](#Konva.Node)
show node. set visible = true

**Inherited from:** `Konva.Node#show`

<a name="Konva.Node+hide"></a>

### circle.hide() ⇒ [<code>Node</code>](#Konva.Node)
hide node.  Hidden nodes are no longer detectable

**Inherited from:** `Konva.Node#hide`

<a name="Konva.Node+getAbsoluteZIndex"></a>

### circle.getAbsoluteZIndex() ⇒ <code>Integer</code>
get absolute z-index which takes into account sibling
 and ancestor indices

**Inherited from:** `Konva.Node#getAbsoluteZIndex`

<a name="Konva.Node+getDepth"></a>

### circle.getDepth() ⇒ <code>Integer</code>
get node depth in node tree.  Returns an integer.
 e.g. Stage depth will always be 0.  Layers will always be 1.  Groups and Shapes will always
 be >= 2

**Inherited from:** `Konva.Node#getDepth`

<a name="Konva.Node+getRelativePointerPosition"></a>

### circle.getRelativePointerPosition() ⇒ [<code>Node</code>](#Konva.Node)
get position of first pointer (like mouse or first touch) relative to local coordinates of current node

**Inherited from:** `Konva.Node#getRelativePointerPosition`

**Example**  
```js
// let's think we have a rectangle at position x = 10, y = 10
// now we clicked at x = 15, y = 15 of the stage
// if you want to know position of the click, related to the rectangle you can use
rect.getRelativePointerPosition();
```
<a name="Konva.Node+getAbsolutePosition"></a>

### circle.getAbsolutePosition(Ancestor) ⇒ [<code>Node</code>](#Konva.Node)
get absolute position of a node. That function can be used to calculate absolute position, but relative to any ancestor

**Inherited from:** `Konva.Node#getAbsolutePosition`


| Param | Type | Description |
| --- | --- | --- |
| Ancestor | <code>Object</code> | optional ancestor node |

**Example**  
```js
// returns absolute position relative to top-left corner of canvas
node.getAbsolutePosition();

// calculate absolute position of node, inside stage
// so stage transforms are ignored
node.getAbsolutePosition(stage)
```
<a name="Konva.Node+move"></a>

### circle.move(change) ⇒ [<code>Node</code>](#Konva.Node)
move node by an amount relative to its current position

**Inherited from:** `Konva.Node#move`


| Param | Type |
| --- | --- |
| change | <code>Object</code> | 
| change.x | <code>Number</code> | 
| change.y | <code>Number</code> | 

**Example**  
```js
// move node in x direction by 1px and y direction by 2px
node.move({
  x: 1,
  y: 2
});
```
<a name="Konva.Node+rotate"></a>

### circle.rotate(theta) ⇒ [<code>Node</code>](#Konva.Node)
rotate node by an amount in degrees relative to its current rotation

**Inherited from:** `Konva.Node#rotate`


| Param | Type |
| --- | --- |
| theta | <code>Number</code> | 

<a name="Konva.Node+moveToTop"></a>

### circle.moveToTop() ⇒ <code>Boolean</code>
move node to the top of its siblings

**Inherited from:** `Konva.Node#moveToTop`

<a name="Konva.Node+moveUp"></a>

### circle.moveUp() ⇒ <code>Boolean</code>
move node up

**Inherited from:** `Konva.Node#moveUp`

**Returns**: <code>Boolean</code> - flag is moved or not  
<a name="Konva.Node+moveDown"></a>

### circle.moveDown() ⇒ <code>Boolean</code>
move node down

**Inherited from:** `Konva.Node#moveDown`

<a name="Konva.Node+moveToBottom"></a>

### circle.moveToBottom() ⇒ <code>Boolean</code>
move node to the bottom of its siblings

**Inherited from:** `Konva.Node#moveToBottom`

<a name="Konva.Node+getAbsoluteOpacity"></a>

### circle.getAbsoluteOpacity() ⇒ <code>Number</code>
get absolute opacity

**Inherited from:** `Konva.Node#getAbsoluteOpacity`

<a name="Konva.Node+moveTo"></a>

### circle.moveTo(newContainer) ⇒ [<code>Node</code>](#Konva.Node)
move node to another container

**Inherited from:** `Konva.Node#moveTo`


| Param | Type |
| --- | --- |
| newContainer | <code>Container</code> | 

**Example**  
```js
// move node from current layer into layer2
node.moveTo(layer2);
```
<a name="Konva.Node+toObject"></a>

### circle.toObject() ⇒ <code>Object</code>
convert Node into an object for serialization.  Returns an object.

**Inherited from:** `Konva.Node#toObject`

<a name="Konva.Node+toJSON"></a>

### circle.toJSON() ⇒ <code>String</code>
convert Node into a JSON string.  Returns a JSON string.

**Inherited from:** `Konva.Node#toJSON`

<a name="Konva.Node+getParent"></a>

### circle.getParent() ⇒ [<code>Node</code>](#Konva.Node)
get parent container

**Inherited from:** `Konva.Node#getParent`

<a name="Konva.Node+findAncestors"></a>

### circle.findAncestors(selector, [includeSelf], [stopNode]) ⇒ <code>Array</code>
get all ancestors (parent then parent of the parent, etc) of the node

**Inherited from:** `Konva.Node#findAncestors`

**Returns**: <code>Array</code> - [ancestors]  

| Param | Type | Description |
| --- | --- | --- |
| selector | <code>String</code> | selector for search |
| [includeSelf] | <code>Boolean</code> | show we think that node is ancestro itself? |
| [stopNode] | [<code>Node</code>](#Konva.Node) | optional node where we need to stop searching (one of ancestors) |

**Example**  
```js
// get one of the parent group
var parentGroups = node.findAncestors('Group');
```
<a name="Konva.Node+findAncestor"></a>

### circle.findAncestor(selector, [includeSelf], [stopNode]) ⇒ [<code>Node</code>](#Konva.Node)
get ancestor (parent or parent of the parent, etc) of the node that match passed selector

**Inherited from:** `Konva.Node#findAncestor`

**Returns**: [<code>Node</code>](#Konva.Node) - ancestor  

| Param | Type | Description |
| --- | --- | --- |
| selector | <code>String</code> | selector for search |
| [includeSelf] | <code>Boolean</code> | show we think that node is ancestro itself? |
| [stopNode] | [<code>Node</code>](#Konva.Node) | optional node where we need to stop searching (one of ancestors) |

**Example**  
```js
// get one of the parent group
var group = node.findAncestors('.mygroup');
```
<a name="Konva.Node+getLayer"></a>

### circle.getLayer() ⇒ [<code>Layer</code>](#Konva.Layer)
get layer ancestor

**Inherited from:** `Konva.Node#getLayer`

<a name="Konva.Node+getStage"></a>

### circle.getStage() ⇒ [<code>Stage</code>](#Konva.Stage)
get stage ancestor

**Inherited from:** `Konva.Node#getStage`

<a name="Konva.Node+fire"></a>

### circle.fire(eventType, [evt], [bubble]) ⇒ [<code>Node</code>](#Konva.Node)
fire event

**Inherited from:** `Konva.Node#fire`


| Param | Type | Description |
| --- | --- | --- |
| eventType | <code>String</code> | event type.  can be a regular event, like click, mouseover, or mouseout, or it can be a custom event, like myCustomEvent |
| [evt] | <code>Event</code> | event object |
| [bubble] | <code>Boolean</code> | setting the value to false, or leaving it undefined, will result in the event  not bubbling.  Setting the value to true will result in the event bubbling. |

**Example**  
```js
// manually fire click event
node.fire('click');

// fire custom event
node.fire('foo');

// fire custom event with custom event object
node.fire('foo', {
  bar: 10
});

// fire click event that bubbles
node.fire('click', null, true);
```
<a name="Konva.Node+getAbsoluteTransform"></a>

### circle.getAbsoluteTransform() ⇒ [<code>Transform</code>](#Konva.Transform)
get absolute transform of the node which takes into
 account its ancestor transforms

**Inherited from:** `Konva.Node#getAbsoluteTransform`

<a name="Konva.Node+getAbsoluteScale"></a>

### circle.getAbsoluteScale() ⇒ <code>Object</code>
get absolute scale of the node which takes into
 account its ancestor scales

**Inherited from:** `Konva.Node#getAbsoluteScale`

**Example**  
```js
// get absolute scale x
var scaleX = node.getAbsoluteScale().x;
```
<a name="Konva.Node+getAbsoluteRotation"></a>

### circle.getAbsoluteRotation() ⇒ <code>Number</code>
get absolute rotation of the node which takes into
 account its ancestor rotations

**Inherited from:** `Konva.Node#getAbsoluteRotation`

**Example**  
```js
// get absolute rotation
var rotation = node.getAbsoluteRotation();
```
<a name="Konva.Node+getTransform"></a>

### circle.getTransform() ⇒ [<code>Transform</code>](#Konva.Transform)
get transform of the node

**Inherited from:** `Konva.Node#getTransform`

<a name="Konva.Node+clone"></a>

### circle.clone(obj) ⇒ [<code>Node</code>](#Konva.Node)
clone node.  Returns a new Node instance with identical attributes.  You can also override
 the node properties with an object literal, enabling you to use an existing node as a template
 for another node

**Inherited from:** `Konva.Node#clone`


| Param | Type | Description |
| --- | --- | --- |
| obj | <code>Object</code> | override attrs |

**Example**  
```js
// simple clone
var clone = node.clone();

// clone a node and override the x position
var clone = rect.clone({
  x: 5
});
```
<a name="Konva.Node+toCanvas"></a>

### circle.toCanvas(config)
converts node into an canvas element.

**Inherited from:** `Konva.Node#toCanvas`


| Param | Type | Description |
| --- | --- | --- |
| config | <code>Object</code> |  |
| config.callback | <code>function</code> | function executed when the composite has completed |
| [config.x] | <code>Number</code> | x position of canvas section |
| [config.y] | <code>Number</code> | y position of canvas section |
| [config.width] | <code>Number</code> | width of canvas section |
| [config.height] | <code>Number</code> | height of canvas section |
| [config.pixelRatio] | <code>Number</code> | pixelRatio of output canvas. Default is 1. You can use that property to increase quality of the image, for example for super hight quality exports or usage on retina (or similar) displays. pixelRatio will be used to multiply the size of exported image. If you export to 500x500 size with pixelRatio = 2, then produced image will have size 1000x1000. |
| [config.imageSmoothingEnabled] | <code>Boolean</code> | set this to false if you want to disable imageSmoothing |

**Example**  
```js
var canvas = node.toCanvas();
```
<a name="Konva.Node+toDataURL"></a>

### circle.toDataURL(config) ⇒ <code>String</code>
Creates a composite data URL (base64 string). If MIME type is not
specified, then "image/png" will result. For "image/jpeg", specify a quality
level as quality (range 0.0 - 1.0)

**Inherited from:** `Konva.Node#toDataURL`


| Param | Type | Description |
| --- | --- | --- |
| config | <code>Object</code> |  |
| [config.mimeType] | <code>String</code> | can be "image/png" or "image/jpeg".  "image/png" is the default |
| [config.x] | <code>Number</code> | x position of canvas section |
| [config.y] | <code>Number</code> | y position of canvas section |
| [config.width] | <code>Number</code> | width of canvas section |
| [config.height] | <code>Number</code> | height of canvas section |
| [config.quality] | <code>Number</code> | jpeg quality.  If using an "image/jpeg" mimeType,  you can specify the quality from 0 to 1, where 0 is very poor quality and 1  is very high quality |
| [config.pixelRatio] | <code>Number</code> | pixelRatio of output image url. Default is 1. You can use that property to increase quality of the image, for example for super hight quality exports or usage on retina (or similar) displays. pixelRatio will be used to multiply the size of exported image. If you export to 500x500 size with pixelRatio = 2, then produced image will have size 1000x1000. |
| [config.imageSmoothingEnabled] | <code>Boolean</code> | set this to false if you want to disable imageSmoothing |

<a name="Konva.Node+toImage"></a>

### circle.toImage(config) ⇒ <code>Promise.&lt;Image&gt;</code>
converts node into an image.  Since the toImage
 method is asynchronous, the resulting image can only be retrieved from the config callback
 or the returned Promise.  toImage is most commonly used
 to cache complex drawings as an image so that they don't have to constantly be redrawn

**Inherited from:** `Konva.Node#toImage`


| Param | Type | Description |
| --- | --- | --- |
| config | <code>Object</code> |  |
| [config.callback] | <code>function</code> | function executed when the composite has completed |
| [config.mimeType] | <code>String</code> | can be "image/png" or "image/jpeg".  "image/png" is the default |
| [config.x] | <code>Number</code> | x position of canvas section |
| [config.y] | <code>Number</code> | y position of canvas section |
| [config.width] | <code>Number</code> | width of canvas section |
| [config.height] | <code>Number</code> | height of canvas section |
| [config.quality] | <code>Number</code> | jpeg quality.  If using an "image/jpeg" mimeType,  you can specify the quality from 0 to 1, where 0 is very poor quality and 1  is very high quality |
| [config.pixelRatio] | <code>Number</code> | pixelRatio of output image. Default is 1. You can use that property to increase quality of the image, for example for super hight quality exports or usage on retina (or similar) displays. pixelRatio will be used to multiply the size of exported image. If you export to 500x500 size with pixelRatio = 2, then produced image will have size 1000x1000. |
| [config.imageSmoothingEnabled] | <code>Boolean</code> | set this to false if you want to disable imageSmoothing |

**Example**  
```js
var image = node.toImage({
  callback(img) {
    // do stuff with img
  }
});
```
<a name="Konva.Node+toBlob"></a>

### circle.toBlob(config) ⇒ <code>Promise.&lt;Blob&gt;</code>
Converts node into a blob.  Since the toBlob method is asynchronous,
 the resulting blob can only be retrieved from the config callback
 or the returned Promise.

**Inherited from:** `Konva.Node#toBlob`


| Param | Type | Description |
| --- | --- | --- |
| config | <code>Object</code> |  |
| [config.callback] | <code>function</code> | function executed when the composite has completed |
| [config.x] | <code>Number</code> | x position of canvas section |
| [config.y] | <code>Number</code> | y position of canvas section |
| [config.width] | <code>Number</code> | width of canvas section |
| [config.height] | <code>Number</code> | height of canvas section |
| [config.pixelRatio] | <code>Number</code> | pixelRatio of output canvas. Default is 1. You can use that property to increase quality of the image, for example for super hight quality exports or usage on retina (or similar) displays. pixelRatio will be used to multiply the size of exported image. If you export to 500x500 size with pixelRatio = 2, then produced image will have size 1000x1000. |
| [config.imageSmoothingEnabled] | <code>Boolean</code> | set this to false if you want to disable imageSmoothing |

**Example**  
```js
var blob = await node.toBlob({});
```
<a name="Konva.Node+getClassName"></a>

### circle.getClassName() ⇒ <code>String</code>
get class name, which may return Stage, Layer, Group, or shape class names like Rect, Circle, Text, etc.

**Inherited from:** `Konva.Node#getClassName`

<a name="Konva.Node+getType"></a>

### circle.getType() ⇒ <code>String</code>
get the node type, which may return Stage, Layer, Group, or Shape

**Inherited from:** `Konva.Node#getType`

<a name="Konva.Node+addName"></a>

### circle.addName(name) ⇒ [<code>Node</code>](#Konva.Node)
add name to node

**Inherited from:** `Konva.Node#addName`


| Param | Type |
| --- | --- |
| name | <code>String</code> | 

**Example**  
```js
node.name('red');
node.addName('selected');
node.name(); // return 'red selected'
```
<a name="Konva.Node+hasName"></a>

### circle.hasName(name) ⇒ <code>Boolean</code>
check is node has name

**Inherited from:** `Konva.Node#hasName`


| Param | Type |
| --- | --- |
| name | <code>String</code> | 

**Example**  
```js
node.name('red');
node.hasName('red');   // return true
node.hasName('selected'); // return false
node.hasName(''); // return false
```
<a name="Konva.Node+removeName"></a>

### circle.removeName(name) ⇒ [<code>Node</code>](#Konva.Node)
remove name from node

**Inherited from:** `Konva.Node#removeName`


| Param | Type |
| --- | --- |
| name | <code>String</code> | 

**Example**  
```js
node.name('red selected');
node.removeName('selected');
node.hasName('selected'); // return false
node.name(); // return 'red'
```
<a name="Konva.Node+setAttr"></a>

### circle.setAttr(attr, val) ⇒ [<code>Node</code>](#Konva.Node)
set attr

**Inherited from:** `Konva.Node#setAttr`


| Param | Type |
| --- | --- |
| attr | <code>String</code> | 
| val | <code>\*</code> | 

**Example**  
```js
node.setAttr('x', 5);
```
<a name="Konva.Node+draw"></a>

### circle.draw() ⇒ [<code>Node</code>](#Konva.Node)
draw both scene and hit graphs.  If the node being drawn is the stage, all of the layers will be cleared and redrawn

**Inherited from:** `Konva.Node#draw`

<a name="Konva.Node+startDrag"></a>

### circle.startDrag()
initiate drag and drop.

**Inherited from:** `Konva.Node#startDrag`

<a name="Konva.Node+stopDrag"></a>

### circle.stopDrag()
stop drag and drop

**Inherited from:** `Konva.Node#stopDrag`

<a name="Konva.Node+isDragging"></a>

### circle.isDragging()
determine if node is currently in drag and drop mode

**Inherited from:** `Konva.Node#isDragging`

<a name="Konva.Node+isClientRectOnScreen"></a>

### circle.isClientRectOnScreen(margin) ⇒ <code>Boolean</code>
determine if node (at least partially) is currently in user-visible area

**Inherited from:** `Konva.Node#isClientRectOnScreen`


| Param | Type | Description |
| --- | --- | --- |
| margin | <code>Number</code> \| <code>Object</code> | optional margin in pixels |
| margin.x | <code>Number</code> |  |
| margin.y | <code>Number</code> |  |

**Example**  
```js
// get index
// default calculations
var isOnScreen = node.isClientRectOnScreen()
// increase object size (or screen size) for cases when objects close to the screen still need to be marked as "visible"
var isOnScreen = node.isClientRectOnScreen({ x: stage.width(), y: stage.height() })
```
<a name="Konva.Node+zIndex"></a>

### circle.zIndex(index) ⇒ <code>Number</code>
get/set zIndex relative to the node's siblings who share the same parent.
Please remember that zIndex is not absolute (like in CSS). It is relative to parent element only.

**Inherited from:** `Konva.Node#zIndex`


| Param | Type |
| --- | --- |
| index | <code>Number</code> | 

**Example**  
```js
// get index
var index = node.zIndex();

// set index
node.zIndex(2);
```
<a name="Konva.Node+absolutePosition"></a>

### circle.absolutePosition(pos) ⇒ <code>Object</code>
get/set node absolute position

**Inherited from:** `Konva.Node#absolutePosition`


| Param | Type |
| --- | --- |
| pos | <code>Object</code> | 
| pos.x | <code>Number</code> | 
| pos.y | <code>Number</code> | 

**Example**  
```js
// get position
var position = node.absolutePosition();

// set position
node.absolutePosition({
  x: 5,
  y: 10
});
```
<a name="Konva.Node+position"></a>

### circle.position(pos) ⇒ <code>Object</code>
get/set node position relative to parent

**Inherited from:** `Konva.Node#position`


| Param | Type |
| --- | --- |
| pos | <code>Object</code> | 
| pos.x | <code>Number</code> | 
| pos.y | <code>Number</code> | 

**Example**  
```js
// get position
var position = node.position();

// set position
node.position({
  x: 5,
  y: 10
});
```
<a name="Konva.Node+x"></a>

### circle.x(x) ⇒ <code>Object</code>
get/set x position

**Inherited from:** `Konva.Node#x`


| Param | Type |
| --- | --- |
| x | <code>Number</code> | 

**Example**  
```js
// get x
var x = node.x();

// set x
node.x(5);
```
<a name="Konva.Node+y"></a>

### circle.y(y) ⇒ <code>Integer</code>
get/set y position

**Inherited from:** `Konva.Node#y`


| Param | Type |
| --- | --- |
| y | <code>Number</code> | 

**Example**  
```js
// get y
var y = node.y();

// set y
node.y(5);
```
<a name="Konva.Node+globalCompositeOperation"></a>

### circle.globalCompositeOperation(type) ⇒ <code>String</code>
get/set globalCompositeOperation of a node. globalCompositeOperation DOESN'T affect hit graph of nodes. So they are still trigger to events as they have default "source-over" globalCompositeOperation.

**Inherited from:** `Konva.Node#globalCompositeOperation`


| Param | Type |
| --- | --- |
| type | <code>String</code> | 

**Example**  
```js
// get globalCompositeOperation
var globalCompositeOperation = shape.globalCompositeOperation();

// set globalCompositeOperation
shape.globalCompositeOperation('source-in');
```
<a name="Konva.Node+opacity"></a>

### circle.opacity(opacity) ⇒ <code>Number</code>
get/set opacity.  Opacity values range from 0 to 1.
 A node with an opacity of 0 is fully transparent, and a node
 with an opacity of 1 is fully opaque

**Inherited from:** `Konva.Node#opacity`


| Param | Type |
| --- | --- |
| opacity | <code>Object</code> | 

**Example**  
```js
// get opacity
var opacity = node.opacity();

// set opacity
node.opacity(0.5);
```
<a name="Konva.Node+name"></a>

### circle.name(name) ⇒ <code>String</code>
get/set name.

**Inherited from:** `Konva.Node#name`


| Param | Type |
| --- | --- |
| name | <code>String</code> | 

**Example**  
```js
// get name
var name = node.name();

// set name
node.name('foo');

// also node may have multiple names (as css classes)
node.name('foo bar');
```
<a name="Konva.Node+id"></a>

### circle.id(id) ⇒ <code>String</code>
get/set id. Id is global for whole page.

**Inherited from:** `Konva.Node#id`


| Param | Type |
| --- | --- |
| id | <code>String</code> | 

**Example**  
```js
// get id
var name = node.id();

// set id
node.id('foo');
```
<a name="Konva.Node+rotation"></a>

### circle.rotation(rotation) ⇒ <code>Number</code>
get/set rotation in degrees

**Inherited from:** `Konva.Node#rotation`


| Param | Type |
| --- | --- |
| rotation | <code>Number</code> | 

**Example**  
```js
// get rotation in degrees
var rotation = node.rotation();

// set rotation in degrees
node.rotation(45);
```
<a name="Konva.Node+scale"></a>

### circle.scale(scale) ⇒ <code>Object</code>
get/set scale

**Inherited from:** `Konva.Node#scale`


| Param | Type |
| --- | --- |
| scale | <code>Object</code> | 
| scale.x | <code>Number</code> | 
| scale.y | <code>Number</code> | 

**Example**  
```js
// get scale
var scale = node.scale();

// set scale
shape.scale({
  x: 2,
  y: 3
});
```
<a name="Konva.Node+scaleX"></a>

### circle.scaleX(x) ⇒ <code>Number</code>
get/set scale x

**Inherited from:** `Konva.Node#scaleX`


| Param | Type |
| --- | --- |
| x | <code>Number</code> | 

**Example**  
```js
// get scale x
var scaleX = node.scaleX();

// set scale x
node.scaleX(2);
```
<a name="Konva.Node+scaleY"></a>

### circle.scaleY(y) ⇒ <code>Number</code>
get/set scale y

**Inherited from:** `Konva.Node#scaleY`


| Param | Type |
| --- | --- |
| y | <code>Number</code> | 

**Example**  
```js
// get scale y
var scaleY = node.scaleY();

// set scale y
node.scaleY(2);
```
<a name="Konva.Node+skew"></a>

### circle.skew(skew) ⇒ <code>Object</code>
get/set skew

**Inherited from:** `Konva.Node#skew`


| Param | Type |
| --- | --- |
| skew | <code>Object</code> | 
| skew.x | <code>Number</code> | 
| skew.y | <code>Number</code> | 

**Example**  
```js
// get skew
var skew = node.skew();

// set skew
node.skew({
  x: 20,
  y: 10
});
```
<a name="Konva.Node+skewX"></a>

### circle.skewX(x) ⇒ <code>Number</code>
get/set skew x

**Inherited from:** `Konva.Node#skewX`


| Param | Type |
| --- | --- |
| x | <code>Number</code> | 

**Example**  
```js
// get skew x
var skewX = node.skewX();

// set skew x
node.skewX(3);
```
<a name="Konva.Node+skewY"></a>

### circle.skewY(y) ⇒ <code>Number</code>
get/set skew y

**Inherited from:** `Konva.Node#skewY`


| Param | Type |
| --- | --- |
| y | <code>Number</code> | 

**Example**  
```js
// get skew y
var skewY = node.skewY();

// set skew y
node.skewY(3);
```
<a name="Konva.Node+offsetX"></a>

### circle.offsetX(x) ⇒ <code>Number</code>
get/set offset x

**Inherited from:** `Konva.Node#offsetX`


| Param | Type |
| --- | --- |
| x | <code>Number</code> | 

**Example**  
```js
// get offset x
var offsetX = node.offsetX();

// set offset x
node.offsetX(3);
```
<a name="Konva.Node+offsetY"></a>

### circle.offsetY(y) ⇒ <code>Number</code>
get/set offset y

**Inherited from:** `Konva.Node#offsetY`


| Param | Type |
| --- | --- |
| y | <code>Number</code> | 

**Example**  
```js
// get offset y
var offsetY = node.offsetY();

// set offset y
node.offsetY(3);
```
<a name="Konva.Node+dragDistance"></a>

### circle.dragDistance(distance) ⇒ <code>Number</code>
get/set drag distance

**Inherited from:** `Konva.Node#dragDistance`


| Param | Type |
| --- | --- |
| distance | <code>Number</code> | 

**Example**  
```js
// get drag distance
var dragDistance = node.dragDistance();

// set distance
// node starts dragging only if pointer moved more then 3 pixels
node.dragDistance(3);
// or set globally
Konva.dragDistance = 3;
```
<a name="Konva.Node+width"></a>

### circle.width(width) ⇒ <code>Number</code>
get/set width

**Inherited from:** `Konva.Node#width`


| Param | Type |
| --- | --- |
| width | <code>Number</code> | 

**Example**  
```js
// get width
var width = node.width();

// set width
node.width(100);
```
<a name="Konva.Node+height"></a>

### circle.height(height) ⇒ <code>Number</code>
get/set height

**Inherited from:** `Konva.Node#height`


| Param | Type |
| --- | --- |
| height | <code>Number</code> | 

**Example**  
```js
// get height
var height = node.height();

// set height
node.height(100);
```
<a name="Konva.Node+listening"></a>

### circle.listening(listening) ⇒ <code>Boolean</code>
get/set listening attr.  If you need to determine if a node is listening or not
  by taking into account its parents, use the isListening() method
  nodes with listening set to false will not be detected in hit graph
  so they will be ignored in container.getIntersection() method

**Inherited from:** `Konva.Node#listening`


| Param | Type | Description |
| --- | --- | --- |
| listening | <code>Boolean</code> | Can be true, or false.  The default is true. |

**Example**  
```js
// get listening attr
var listening = node.listening();

// stop listening for events, remove node and all its children from hit graph
node.listening(false);

// listen to events according to the parent
node.listening(true);
```
<a name="Konva.Node+preventDefault"></a>

### circle.preventDefault(preventDefault) ⇒ <code>Boolean</code>
get/set preventDefault
By default all shapes will prevent default behavior
of a browser on a pointer move or tap.
that will prevent native scrolling when you are trying to drag&drop a node
but sometimes you may need to enable default actions
in that case you can set the property to false

**Inherited from:** `Konva.Node#preventDefault`


| Param | Type |
| --- | --- |
| preventDefault | <code>Boolean</code> | 

**Example**  
```js
// get preventDefault
var shouldPrevent = shape.preventDefault();

// set preventDefault
shape.preventDefault(false);
```
<a name="Konva.Node+filters"></a>

### circle.filters(filters) ⇒ <code>Array</code>
get/set filters.  Filters are applied to cached canvases

**Inherited from:** `Konva.Node#filters`


| Param | Type | Description |
| --- | --- | --- |
| filters | <code>Array</code> | array of filters |

**Example**  
```js
// get filters
var filters = node.filters();

// set a single filter
node.cache();
node.filters([Konva.Filters.Blur]);

// set multiple filters
node.cache();
node.filters([
  Konva.Filters.Blur,
  Konva.Filters.Sepia,
  Konva.Filters.Invert
]);
```
<a name="Konva.Node+visible"></a>

### circle.visible(visible) ⇒ <code>Boolean</code>
get/set visible attr.  Can be true, or false.  The default is true.
  If you need to determine if a node is visible or not
  by taking into account its parents, use the isVisible() method

**Inherited from:** `Konva.Node#visible`


| Param | Type |
| --- | --- |
| visible | <code>Boolean</code> | 

**Example**  
```js
// get visible attr
var visible = node.visible();

// make invisible
node.visible(false);

// make visible (according to the parent)
node.visible(true);
```
<a name="Konva.Node+transformsEnabled"></a>

### circle.transformsEnabled(enabled) ⇒ <code>String</code>
get/set transforms that are enabled.  Can be "all", "none", or "position".  The default
 is "all"

**Inherited from:** `Konva.Node#transformsEnabled`


| Param | Type |
| --- | --- |
| enabled | <code>String</code> | 

**Example**  
```js
// enable position transform only to improve draw performance
node.transformsEnabled('position');

// enable all transforms
node.transformsEnabled('all');
```
<a name="Konva.Node+size"></a>

### circle.size(size) ⇒ <code>Object</code>
get/set node size

**Inherited from:** `Konva.Node#size`


| Param | Type |
| --- | --- |
| size | <code>Object</code> | 
| size.width | <code>Number</code> | 
| size.height | <code>Number</code> | 

**Example**  
```js
// get node size
var size = node.size();
var width = size.width;
var height = size.height;

// set size
node.size({
  width: 100,
  height: 200
});
```
<a name="Konva.Node+dragBoundFunc"></a>

### circle.dragBoundFunc(dragBoundFunc) ⇒ <code>function</code>
get/set drag bound function.  This is used to override the default
 drag and drop position.

**Inherited from:** `Konva.Node#dragBoundFunc`


| Param | Type |
| --- | --- |
| dragBoundFunc | <code>function</code> | 

**Example**  
```js
// get drag bound function
var dragBoundFunc = node.dragBoundFunc();

// create vertical drag and drop
node.dragBoundFunc(function(pos){
  // important pos - is absolute position of the node
  // you should return absolute position too
  return {
    x: this.absolutePosition().x,
    y: pos.y
  };
});
```
<a name="Konva.Node+draggable"></a>

### circle.draggable(draggable) ⇒ <code>Boolean</code>
get/set draggable flag

**Inherited from:** `Konva.Node#draggable`


| Param | Type |
| --- | --- |
| draggable | <code>Boolean</code> | 

**Example**  
```js
// get draggable flag
var draggable = node.draggable();

// enable drag and drop
node.draggable(true);

// disable drag and drop
node.draggable(false);
```
<a name="Konva.Node+to"></a>

### circle.to([params])
Tween node properties. Shorter usage of [Tween](#Konva.Tween) object.

**Inherited from:** `Konva.Node#to`


| Param | Type | Description |
| --- | --- | --- |
| [params] | <code>Object</code> | tween params |

**Example**  
```js
circle.to({
  x : 50,
  duration : 0.5,
  onUpdate: () => console.log('props updated'),
  onFinish: () => console.log('finished'),
});
```
<a name="Konva.Node+blurRadius"></a>

### circle.blurRadius(radius) ⇒ <code>Integer</code>
get/set blur radius. Use with [Blur](#Konva.Filters.Blur) filter

**Inherited from:** `Konva.Node#blurRadius`


| Param | Type |
| --- | --- |
| radius | <code>Integer</code> | 

<a name="Konva.Node+brightness"></a>

### circle.brightness(brightness) ⇒ <code>Number</code>
get/set filter brightness.  The brightness is a number between -1 and 1.&nbsp; Positive values
 brighten the pixels and negative values darken them. Use with [Brighten](#Konva.Filters.Brighten) filter.

**Inherited from:** `Konva.Node#brightness`


| Param | Type | Description |
| --- | --- | --- |
| brightness | <code>Number</code> | value between -1 and 1 |

<a name="Konva.Node+contrast"></a>

### circle.contrast(contrast) ⇒ <code>Number</code>
get/set filter contrast.  The contrast is a number between -100 and 100.
Use with [Contrast](#Konva.Filters.Contrast) filter.

**Inherited from:** `Konva.Node#contrast`


| Param | Type | Description |
| --- | --- | --- |
| contrast | <code>Number</code> | value between -100 and 100 |

<a name="Konva.Node+embossStrength"></a>

### circle.embossStrength(level) ⇒ <code>Number</code>
get/set emboss strength. Use with [Emboss](#Konva.Filters.Emboss) filter.

**Inherited from:** `Konva.Node#embossStrength`


| Param | Type | Description |
| --- | --- | --- |
| level | <code>Number</code> | between 0 and 1.  Default is 0.5 |

<a name="Konva.Node+embossWhiteLevel"></a>

### circle.embossWhiteLevel(embossWhiteLevel) ⇒ <code>Number</code>
get/set emboss white level. Use with [Emboss](#Konva.Filters.Emboss) filter.

**Inherited from:** `Konva.Node#embossWhiteLevel`


| Param | Type | Description |
| --- | --- | --- |
| embossWhiteLevel | <code>Number</code> | between 0 and 1.  Default is 0.5 |

<a name="Konva.Node+embossDirection"></a>

### circle.embossDirection(embossDirection) ⇒ <code>String</code>
get/set emboss direction. Use with [Emboss](#Konva.Filters.Emboss) filter.

**Inherited from:** `Konva.Node#embossDirection`


| Param | Type | Description |
| --- | --- | --- |
| embossDirection | <code>String</code> | can be top-left, top, top-right, right, bottom-right, bottom, bottom-left or left   The default is top-left |

<a name="Konva.Node+embossBlend"></a>

### circle.embossBlend(embossBlend) ⇒ <code>Boolean</code>
get/set emboss blend. Use with [Emboss](#Konva.Filters.Emboss) filter.

**Inherited from:** `Konva.Node#embossBlend`


| Param | Type |
| --- | --- |
| embossBlend | <code>Boolean</code> | 

<a name="Konva.Node+enhance"></a>

### circle.enhance(amount) ⇒ <code>Float</code>
get/set enhance. Use with [Enhance](#Konva.Filters.Enhance) filter. -1 to 1 values

**Inherited from:** `Konva.Node#enhance`


| Param | Type |
| --- | --- |
| amount | <code>Float</code> | 

<a name="Konva.Node+hue"></a>

### circle.hue(hue) ⇒ <code>Number</code>
get/set hsv hue in degrees. Use with [HSV](#Konva.Filters.HSV) or [HSL](#Konva.Filters.HSL) filter.

**Inherited from:** `Konva.Node#hue`


| Param | Type | Description |
| --- | --- | --- |
| hue | <code>Number</code> | value between 0 and 359 |

<a name="Konva.Node+saturation"></a>

### circle.saturation(saturation) ⇒ <code>Number</code>
get/set hsv saturation. Use with [HSV](#Konva.Filters.HSV) or [HSL](#Konva.Filters.HSL) filter.

**Inherited from:** `Konva.Node#saturation`


| Param | Type | Description |
| --- | --- | --- |
| saturation | <code>Number</code> | 0 is no change, -1.0 halves the saturation, 1.0 doubles, etc.. |

<a name="Konva.Node+luminance"></a>

### circle.luminance(value) ⇒ <code>Number</code>
get/set hsl luminance. Use with [HSL](#Konva.Filters.HSL) filter.

**Inherited from:** `Konva.Node#luminance`


| Param | Type | Description |
| --- | --- | --- |
| value | <code>Number</code> | from -1 to 1 |

<a name="Konva.Node+value"></a>

### circle.value(value) ⇒ <code>Number</code>
get/set hsv value. Use with [HSV](#Konva.Filters.HSV) filter.

**Inherited from:** `Konva.Node#value`


| Param | Type | Description |
| --- | --- | --- |
| value | <code>Number</code> | 0 is no change, -1.0 halves the value, 1.0 doubles, etc.. |

<a name="Konva.Node+kaleidoscopePower"></a>

### circle.kaleidoscopePower(power) ⇒ <code>Integer</code>
get/set kaleidoscope power. Use with [Konva.Filters.Kaleidoscope](Konva.Filters.Kaleidoscope) filter.

**Inherited from:** `Konva.Node#kaleidoscopePower`


| Param | Type | Description |
| --- | --- | --- |
| power | <code>Integer</code> | of kaleidoscope |

<a name="Konva.Node+kaleidoscopeAngle"></a>

### circle.kaleidoscopeAngle(degrees) ⇒ <code>Integer</code>
get/set kaleidoscope angle. Use with [Konva.Filters.Kaleidoscope](Konva.Filters.Kaleidoscope) filter.

**Inherited from:** `Konva.Node#kaleidoscopeAngle`


| Param | Type |
| --- | --- |
| degrees | <code>Integer</code> | 

<a name="Konva.Node+noise"></a>

### circle.noise(noise) ⇒ <code>Number</code>
get/set noise amount.  Must be a value between 0 and 1. Use with [Noise](#Konva.Filters.Noise) filter.

**Inherited from:** `Konva.Node#noise`


| Param | Type |
| --- | --- |
| noise | <code>Number</code> | 

<a name="Konva.Node+pixelSize"></a>

### circle.pixelSize(pixelSize) ⇒ <code>Integer</code>
get/set pixel size. Use with [Pixelate](#Konva.Filters.Pixelate) filter.

**Inherited from:** `Konva.Node#pixelSize`


| Param | Type |
| --- | --- |
| pixelSize | <code>Integer</code> | 

<a name="Konva.Node+levels"></a>

### circle.levels(level) ⇒ <code>Number</code>
get/set levels.  Must be a number between 0 and 1.  Use with [Posterize](#Konva.Filters.Posterize) filter.

**Inherited from:** `Konva.Node#levels`


| Param | Type | Description |
| --- | --- | --- |
| level | <code>Number</code> | between 0 and 1 |

<a name="Konva.Node+red"></a>

### circle.red(red) ⇒ <code>Integer</code>
get/set filter red value. Use with [RGB](#Konva.Filters.RGB) filter.

**Inherited from:** `Konva.Node#red`


| Param | Type | Description |
| --- | --- | --- |
| red | <code>Integer</code> | value between 0 and 255 |

<a name="Konva.Node+green"></a>

### circle.green(green) ⇒ <code>Integer</code>
get/set filter green value. Use with [RGB](#Konva.Filters.RGB) filter.

**Inherited from:** `Konva.Node#green`


| Param | Type | Description |
| --- | --- | --- |
| green | <code>Integer</code> | value between 0 and 255 |

<a name="Konva.Node+blue"></a>

### circle.blue(blue) ⇒ <code>Integer</code>
get/set filter blue value. Use with [RGB](#Konva.Filters.RGB) filter.

**Inherited from:** `Konva.Node#blue`


| Param | Type | Description |
| --- | --- | --- |
| blue | <code>Integer</code> | value between 0 and 255 |

<a name="Konva.Node+alpha"></a>

### circle.alpha(alpha) ⇒ <code>Float</code>
get/set filter alpha value. Use with [RGBA](#Konva.Filters.RGBA) filter.

**Inherited from:** `Konva.Node#alpha`


| Param | Type | Description |
| --- | --- | --- |
| alpha | <code>Float</code> | value between 0 and 1 |

<a name="Konva.Node+threshold"></a>

### circle.threshold(threshold) ⇒ <code>Number</code>
get/set threshold.  Must be a value between 0 and 1. Use with [Threshold](#Konva.Filters.Threshold) or [Mask](#Konva.Filters.Mask) filter.

**Inherited from:** `Konva.Node#threshold`


| Param | Type |
| --- | --- |
| threshold | <code>Number</code> | 

