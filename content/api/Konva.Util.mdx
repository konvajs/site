---
title: Konva.Util
sidebar_label: Util
slug: /api/Konva.Util.html
description: "Konva.Util namespace reference for Konva.js: properties, methods and configuration for working with Util on HTML5 Canvas."

---

# Util

## Own Methods

### getRandomColor() {#getRandomColor}

return random hex color

**Example:**

```javascript
shape.fill(Konva.Util.getRandomColor());
```

### isCanvasFarblingActive() {#isCanvasFarblingActive}

Check if canvas farbling is active (e.g., Brave browser fingerprinting protection)

**Returns:** `Boolean` 

### getHitColor() {#getHitColor}

Get a random color for hit detection (snapped to the hit color grid)

**Returns:** `String` <code>hex color string</code>

### getHitColorKey(r, g, b) {#getHitColorKey}

Get hit color key from RGB values (snapped to the hit color grid)

**Parameters:**

- `r` (Number): red component (0-255)
- `g` (Number): green component (0-255)
- `b` (Number): blue component (0-255)

**Returns:** `String` <code>hex color key string</code>

### getSnappedHexColor(hex) {#getSnappedHexColor}

Snap a hex color to the hit color grid

**Parameters:**

- `hex` (String): hex color string (e.g., "#ff00ff")

**Returns:** `String` <code>snapped hex color string</code>

### getRGB(color) {#getRGB}

get RGB components of a color

**Parameters:**

- `color` (String)

**Example:**

```javascript
// each of the following examples return {r:0, g:0, b:255}
var rgb = Konva.Util.getRGB('blue');
var rgb = Konva.Util.getRGB('#0000ff');
var rgb = Konva.Util.getRGB('rgb(0,0,255)');
```

### haveIntersection(r1, r2) {#haveIntersection}

check intersection of two client rectangles

**Parameters:**

- `r1` (Object): \{ x, y, width, height \} client rectangle
- `r2` (Object): \{ x, y, width, height \} client rectangle

**Example:**

```javascript
const overlapping = Konva.Util.haveIntersection(shape1.getClientRect(), shape2.getClientRect());
```

